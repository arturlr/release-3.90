@model IList<Nop.Core.Domain.News.NewsItem>

@{
    ViewData["Title"] = "Blog";
}

<h1 style="margin-bottom: 2rem;">Blog</h1>

@if (Model == null || !Model.Any())
{
    <p>No blog posts available.</p>
}
else
{
    <div style="display: grid; gap: 20px;">
        @foreach (var post in Model)
        {
            <div style="border: 1px solid #ddd; padding: 20px; border-radius: 4px;">
                <h2 style="margin-bottom: 0.5rem;">
                    <a href="/Blog/BlogPost/@post.Id" style="color: #333; text-decoration: none;">@post.Title</a>
                </h2>
                <p style="color: #999; font-size: 0.9rem; margin-bottom: 1rem;">
                    @post.CreatedOnUtc.ToString("MMMM dd, yyyy")
                </p>
                <p style="color: #666; line-height: 1.6;">@post.Short</p>
                <a href="/Blog/BlogPost/@post.Id" style="color: #007bff;">Read more &rarr;</a>
            </div>
        }
    </div>

    <div style="text-align: center; margin-top: 2rem;">
        <a href="/Blog?page=1" class="btn btn-secondary">Load More</a>
    </div>
}
