# Implementation Plan: .NET 10 Modernization of nopCommerce release-3.90

## Overview

This plan ports every in-scope project of nopCommerce release-3.90 from .NET Framework 4.5.1 (classic MSBuild, `packages.config`, ASP.NET MVC 5 / System.Web) to **.NET 10** (`net10.0`, SDK-style projects, `PackageReference`, ASP.NET Core MVC).

The plan applies the **per-project migration recipe** from the design (SDK-style `.csproj` at `net10.0` → `packages.config` to `PackageReference` → config migration → remove obsolete files → rewrite unavailable APIs → fix project references → **clean-compile gate**) in **strict leaf-first dependency order**:

```
Nop.Core → Nop.Data → Nop.Services → Nop.Web.Framework → {Nop.Web, Nop.Admin} → Plugins (×20) → Tests (×5)
```

`{Nop.Web, Nop.Admin}` denotes a **parallelizable sibling pair**: `Nop.Admin` (`src/Presentation/Nop.Web/Administration/Nop.Admin.csproj`) does not reference `Nop.Web`, and `Nop.Web` does not reference `Nop.Admin`; both depend only on `Nop.Web.Framework` plus the three libraries (design §6). Once the `Nop.Web.Framework` gate passes, the two may proceed in either order or concurrently — but **both** must clean-compile before any plugin is migrated.

Each project's task group ends with a **clean-compile gate** (zero compiler errors) that MUST pass before any dependent group begins. `Clean_Compile` — not test execution — is the completion criterion for every stage (Req 3.3). Coverage is exhaustive across all **31 projects**: 3 libraries (`Nop.Core`, `Nop.Data`, `Nop.Services`), 3 presentation projects (`Nop.Web.Framework`, `Nop.Web`, `Nop.Admin`), all 20 plugins, and all 5 test projects.

## Tasks

- [x] 1. Solution-wide migration scaffolding
  - [x] 1.1 Establish central package management and shared build props
    - Create `src/Directory.Packages.props` with `<ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>` and net10.0-compatible `<PackageVersion>` entries for the full dependency surface (Autofac + Autofac.Extensions.DependencyInjection, AutoMapper, Newtonsoft.Json/System.Text.Json, Microsoft.Extensions.Caching.Memory, Microsoft.Extensions.Configuration, EF Core + EF Core SqlServer, StackExchange.Redis, a maintained RedLock package, SixLabors.ImageSharp)
    - Create `src/Directory.Build.props` setting shared properties (`TargetFramework=net10.0`, `Nullable=disable`, `ImplicitUsings=disable`, `GenerateAssemblyInfo=false`) to keep per-project `.csproj` files minimal
    - _Requirements: 1.1, 1.2, 1.3, 5.2_

- [x] 2. Migrate Nop.Core (Leaf_Project)
  - [x] 2.1 Convert Nop.Core.csproj to SDK-style net10.0
    - Replace the classic `.csproj` with `<Project Sdk="Microsoft.NET.Sdk">`, `<TargetFramework>net10.0</TargetFramework>`
    - Delete explicit `<Compile Include=.../>` list (rely on implicit globbing); drop `ProjectGuid`, `TargetFrameworkVersion`, Configuration/Platform blocks, `Import *.targets`, and BCL `<Reference Include="System.*"/>` entries
    - _Requirements: 1.1, 1.2, 5.1_
  - [x] 2.2 Convert Nop.Core packages.config to PackageReference
    - Translate direct dependencies to `PackageReference` (via central versions); trim transitive entries (`Microsoft.Web.Infrastructure`, individual `System.Web.*`); advance net45-era pins to net10.0-compatible versions; delete `packages.config`
    - _Requirements: 1.3, 1.5_
  - [x] 2.3 Migrate Nop.Core configuration and remove obsolete files
    - Remove `app.config` (BCL/binding-redirect noise handled by SDK); remove `Properties/AssemblyInfo.cs` if superseded by generated assembly info; surface any genuine settings through `IConfiguration`/options
    - _Requirements: 1.4, 1.5_
  - [x] 2.4 Rewrite unavailable-API usages in Nop.Core
    - Re-base caching implementations behind existing `ICacheManager`: `MemoryCacheManager` from `System.Runtime.Caching` → `Microsoft.Extensions.Caching.Memory.IMemoryCache` (map expirations to `MemoryCacheEntryOptions`, clear-all via `CancellationChangeToken`); `PerRequestCacheManager` from `HttpContext.Current.Items` → `IHttpContextAccessor.HttpContext.Items`; keep `NopNullCache`; keep `ICacheManager` signatures stable
    - Reimplement `IWebHelper`/`WebHelper` against `Microsoft.AspNetCore.Http` `HttpContext` (via `IHttpContextAccessor`) instead of `System.Web`
    - Replace `Fakes/Fake*` System.Web seams with `DefaultHttpContext`-based abstractions or remove where superseded
    - _Requirements: 4.2, 5.1, 5.3_
  - [x] 2.5 Clean-compile gate — Nop.Core
    - Build `src/Libraries/Nop.Core/Nop.Core.csproj -c Debug` using the containerized .NET 10 SDK command in `build-environment.md`; resolve all errors to zero before unlocking Nop.Data; verify no `System.Web*` references remain
    - _Requirements: 3.1, 3.2, 3.3_
  - [ ]* 2.6 Migrate Nop.Core.Tests to SDK-style net10.0
    - Convert `src/Tests/Nop.Core.Tests` to SDK-style net10.0, packages.config → PackageReference (test SDK + runner), `App.config` removed, `ProjectReference` → migrated Nop.Core; require clean compile
    - _Requirements: 5.6, 1.1, 1.2, 1.3, 3.1_

- [x] 3. Migrate Nop.Data (EF6 → EF Core)
  - [x] 3.1 Convert Nop.Data.csproj to SDK-style net10.0 and migrate packages
    - SDK-style conversion (`Microsoft.NET.Sdk`, `net10.0`); drop legacy globbing/GUID/BCL refs; packages.config → PackageReference; replace `EntityFramework` 6 with `Microsoft.EntityFrameworkCore` + `Microsoft.EntityFrameworkCore.SqlServer`; delete `packages.config`
    - Remove SQL CE dependencies (`EntityFramework.SqlServerCompact`, `Microsoft.SqlServer.Compact`) and delete `SqlCeDataProvider.cs` (no EF Core provider; pruned from scope)
    - Remove `app.config`; fix `ProjectReference` to migrated Nop.Core
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 5.3_
  - [x] 3.2 Port DbContext, mappings, and repository to EF Core
    - `NopObjectContext`: `System.Data.Entity.DbContext` → `Microsoft.EntityFrameworkCore.DbContext`; keep `IDbContext`, `IRepository<T>`, `EfRepository<T>` public shape so dependents compile unchanged
    - Convert fluent `Mapping/*` `EntityTypeConfiguration<T>` → `IEntityTypeConfiguration<T>` applied via `ModelBuilder.ApplyConfigurationsFromAssembly`
    - Port `DbContextExtensions`/`DataReaderExtensions` raw-SQL/stored-proc helpers to `FromSqlRaw`/`ExecuteSqlRaw`/`SqlQuery`; port `Initializers/*` to EF Core model creation (`EnsureCreated`/migrations)
    - Re-base `SqlServerDataProvider` on EF Core SqlServer
    - _Requirements: 4.2, 5.1, 5.3_
  - [x] 3.3 Clean-compile gate — Nop.Data
    - Build `src/Libraries/Nop.Data/Nop.Data.csproj -c Debug` using the containerized .NET 10 SDK command in `build-environment.md`; resolve to zero errors before unlocking Nop.Services
    - _Requirements: 3.1, 3.2, 3.3_
  - [ ]* 3.4 Migrate Nop.Data.Tests to SDK-style net10.0
    - Convert `src/Tests/Nop.Data.Tests`; packages.config → PackageReference; remove `App.config`; `ProjectReference` → migrated Nop.Data/Nop.Core; require clean compile
    - _Requirements: 5.6, 1.1, 1.2, 1.3, 3.1_

- [x] 4. Migrate Nop.Services
  - [x] 4.1 Convert Nop.Services.csproj to SDK-style net10.0 and migrate packages
    - SDK-style conversion at `net10.0`; packages.config → PackageReference (AutoMapper advanced, Autofac advanced); delete `packages.config`; remove `app.config`; fix `ProjectReference`s to migrated Nop.Core and Nop.Data
    - Add the `SixLabors.ImageSharp` `PackageReference` and **drop the `ImageResizer` and `ImageResizer.Plugins.PrettyGifs` references** (no net10.0 release exists); drop the `System.Drawing` BCL `<Reference>` — GIF quantization is covered by ImageSharp's built-in encoder (design §7)
    - Keep the target framework as cross-platform `net10.0` (not `net10.0-windows`) now that imaging no longer depends on `System.Drawing`
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 5.8, 5.10_
  - [x] 4.2 Rewrite unavailable-API usages across Nop.Services
    - Update every source file using APIs unavailable in net10.0: `ConfigurationManager` → injected `IConfiguration`/options; `HttpContext.Current` → `IHttpContextAccessor`; `System.Web`-based helpers → ASP.NET Core abstractions; AutoMapper API updates; caching against stable `ICacheManager`
    - Replace `ImageResizer` and `System.Drawing` in `Media/PictureService.cs` with SixLabors.ImageSharp: `ImageBuilder`/`ResizeSettings` → `Image.Load` + `image.Mutate(x => x.Resize(new ResizeOptions { … }))` + `image.Save(...)` with an ImageSharp encoder; `Bitmap`/`Graphics`/`InterpolationMode`/`PixelFormat` → `Image<TPixel>` + `Mutate`/`Resize` with an `IResampler` (design §7)
    - Replace `System.Drawing` usage in `ExportImport/ExportManager.cs` with SixLabors.ImageSharp equivalents
    - Preserve `IDependencyRegistrar` implementations (registration churn deferred to DI integration in Web.Framework)
    - _Requirements: 4.2, 5.1, 5.3, 5.8, 5.9, 5.11_
  - [x] 4.3 Clean-compile gate — Nop.Services
    - Build `src/Libraries/Nop.Services/Nop.Services.csproj -c Debug` using the containerized .NET 10 SDK command in `build-environment.md`; resolve to zero errors before unlocking Nop.Web.Framework; verify no `ImageResizer` or `System.Drawing` references remain
    - _Requirements: 3.1, 3.2, 3.3_
  - [ ]* 4.4 Migrate Nop.Services.Tests to SDK-style net10.0
    - Convert `src/Tests/Nop.Services.Tests`; packages.config → PackageReference; remove `App.config`; `ProjectReference`s → migrated Nop.Services/Nop.Data/Nop.Core; require clean compile
    - _Requirements: 5.6, 1.1, 1.2, 1.3, 3.1_
  - [x] 4.5 Migrate shared Nop.Tests helper project to SDK-style net10.0
    - Convert `src/Tests/Nop.Tests` (shared test infrastructure) to SDK-style net10.0; packages.config → PackageReference; replace System.Web fakes with `DefaultHttpContext`-based fakes; fix `ProjectReference`s; require clean compile
    - _Requirements: 5.6, 5.3, 1.1, 1.2, 1.3, 3.1_

- [x] 5. Checkpoint — libraries migrated
  - Ensure Nop.Core, Nop.Data, Nop.Services all clean-compile and dependent references point only at migrated SDK-style projects. Ensure all tests pass, ask the user if questions arise.
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 3.1, 3.3_

- [x] 6. Port Nop.Web.Framework to ASP.NET Core
  - [x] 6.1 Convert Nop.Web.Framework.csproj to SDK-style net10.0 and migrate packages
    - SDK-style conversion (`Microsoft.NET.Sdk`, with Razor/ASP.NET Core MVC framework reference as needed) at `net10.0`; packages.config → PackageReference; replace `Microsoft.AspNet.Mvc/Razor/WebPages`, `Autofac.Mvc5` with ASP.NET Core MVC + `Autofac.Extensions.DependencyInjection`; delete `packages.config`; fix `ProjectReference`s to migrated libraries
    - _Requirements: 1.1, 1.2, 1.3, 2.4, 4.1, 4.2_
  - [x] 6.2 Port HTTP context, WebHelper, and controllers/filters
    - Replace `System.Web` `HttpContext.Current`/`HttpRequest`/`HttpResponse`/session usages with `Microsoft.AspNetCore.Http` via `IHttpContextAccessor`
    - `System.Web.Mvc.Controller` → `Microsoft.AspNetCore.Mvc.Controller`; map `ActionResult`/`JsonResult`/`RedirectResult`; port filters (`ActionFilterAttribute`, `AuthorizeAttribute`) to ASP.NET Core filters/policy-based auth
    - _Requirements: 4.1, 4.2, 4.3_
  - [x] 6.3 Port Razor infrastructure and HTML/URL helpers
    - `WebViewPage`/`@model` base → `RazorPage<TModel>`; `System.Web.Mvc.HtmlHelper` → `IHtmlHelper`, `UrlHelper` → `IUrlHelper`; `@Html.Action`/`[ChildActionOnly]` → View Components; port custom view-page extensions and templates to Tag Helpers where appropriate
    - _Requirements: 4.1, 4.4, 4.2_
  - [x] 6.4 Port routing, modules/handlers, and DI integration
    - `IRouteProvider`/`RouteCollection`/area registration → ASP.NET Core endpoint routing (`MapControllerRoute`, area conventions) over `IEndpointRouteBuilder`
    - `IHttpModule`/`IHttpHandler` (URL rewrite/SEO, culture/localization, auth, install-mode redirect) → middleware components preserving ordering
    - Integrate Autofac via `Autofac.Extensions.DependencyInjection`; remove `Autofac.Integration.Mvc` per-request plumbing (`AutofacDependencyResolver`, `RequestLifetimeScopeProvider`); keep `IEngine`/`NopEngine`/`ContainerManager` shape
    - _Requirements: 4.2, 4.5, 4.6_
  - [x] 6.5 Migrate Nop.Web.Framework configuration and remove obsolete files
    - Move settings to `IConfiguration`/typed options; remove obsolete `AssemblyInfo`/`app.config`/View `web.config` fragments owned by the framework project
    - _Requirements: 1.4, 1.5, 5.3_
  - [x] 6.6 Clean-compile gate — Nop.Web.Framework
    - Build `src/Presentation/Nop.Web.Framework/Nop.Web.Framework.csproj -c Debug` using the containerized .NET 10 SDK command in `build-environment.md`; resolve to zero errors before unlocking **both** Nop.Web and Nop.Admin; verify no `System.Web*` references remain
    - _Requirements: 3.1, 3.2, 3.3, 4.2_

- [ ] 7. Port Nop.Web to ASP.NET Core
  - [x] 7.1 Convert Nop.Web.csproj to SDK-style Web project at net10.0
    - Convert to `<Project Sdk="Microsoft.NET.Sdk.Web">`, `net10.0`; packages.config → PackageReference; delete `packages.config`; fix `ProjectReference`s to all migrated upstream projects
    - _Requirements: 1.1, 1.2, 1.3, 2.4, 4.1_
  - [x] 7.2 Replace hosting model (Global.asax → Program/Startup)
    - Replace `Global.asax`/`Global.asax.cs` and `System.Web` application events with `Program.cs` (`WebApplication` generic host) and `ConfigureServices`/`Configure`; invoke `IEngine`/`NopEngine`/`EngineContext` startup from host; wire `UseServiceProviderFactory(new AutofacServiceProviderFactory())` + `ConfigureContainer`
    - _Requirements: 4.1, 4.5, 4.6_
  - [x] 7.3 Port Nop.Web controllers, views, routing, and pipeline
    - `System.Web.Mvc.Controller` → ASP.NET Core controllers across all Nop.Web controllers (Req 4.3)
    - Port all `.cshtml` views to ASP.NET Core Razor; replace `Views/web.config` + `_ViewStart` with `_ViewImports.cshtml`/`_ViewStart.cshtml`; port display/editor templates (Req 4.4)
    - Register routing over endpoint routing and register former modules/handlers as middleware in correct order (Req 4.6)
    - **RESULT: Nop.Web reached 0 errors** (from the 1110 baseline) — the 7.6 gate criterion is met. 15 warnings remain, all pre-existing (10 upstream `SYSLIB*` in Nop.Core/Nop.Services, 5 `CS0618` on FluentValidation 7.x's obsolete `Custom(...)` in unmodified `Validators/` files). See runtime-deferrals.md §27–§30.
    - **SCOPE ADDITIONS forced by the port:** a `Html.Action` child-action bridge (`Extensions/ChildActionExtensions.cs`) — unavoidable because five call sites name the controller/action from plugin data at runtime; `Components/WidgetViewComponent.cs` (closes deferral 32); `Views/_ViewImports.cshtml`; `Extensions/SessionExtensions.cs`; `Extensions/ViewCompatibilityExtensions.cs`. `Views/Widget/WidgetsByZone.cshtml` moved to `Views/Shared/Components/Widget/Default.cshtml`.
    - **CORRECTION to an upstream instruction:** the recommended `.WithOrder(1000)` on `GenericUrlRouteProvider`'s seven name-only `{SeName}` routes is *backwards* — a live probe showed it **creates** the `AmbiguousMatchException` it was meant to prevent. `SuppressMatchingMetadata` is used instead. See runtime-deferrals.md §28.1.
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.6_
  - [x] 7.4 Migrate web.config to appsettings.json / IConfiguration
    - Split `web.config`: appSettings/connectionStrings/custom sections → `appsettings.json` bound via `IConfiguration`/`IOptions<T>`; `system.web`/`system.webServer` module & handler registrations → middleware in `Program.cs`; rewrite `ConfigurationManager.*` call-sites; keep only the ANCM hosting shim `web.config` if needed; load `DataSettings` via new config path
    - **Remove the `resizer` `configSections` declaration and its element** (`<section name="resizer" type="ImageResizer.ResizerSection,ImageResizer" />`) from `src/Presentation/Nop.Web/Web.config` — ImageSharp is configured in code/options, not via a config section (design §7)
    - **RESULT: still 0 errors / 15 warnings** — the 7.3 baseline is preserved byte-for-byte (the 5 `CS0618` FluentValidation warnings are in `Validators/` files this task did not touch; the pin is design §9). **Six deferrals closed** (4/7.2-4 `appsettings.json`; 16/7.16 EU VAT endpoint; 33/14.33 cache busting; 39/7.1-4 publish shaping; 40/7.1-5 static assets; the second half of 7.20), **one closed by decision** (36/7.1-1 Redis session), **two opened** (7.4-1, 7.4-2). See runtime-deferrals.md §31–§36.
    - **NEW FILES:** `appsettings.json` (every value traced to the legacy element it replaces); `Infrastructure/NopStaticFileProvider.cs` + `Infrastructure/NopStaticFilesExtensions.cs`. `Web.config` reduced to the IIS/ANCM shim and **renamed to lowercase `web.config`** — see the correction below. `Web.Debug.config` / `Web.Release.config` deleted (XDT boilerplate the Web SDK does not run). `Content/Images/Thumbs/placeholder.txt` restored.
    - **DECISION on deferral 40 (static assets), which 7.3 left open:** the *provider* moved, not the files — an allow-listed `IFileProvider` over the content root, installed onto `IWebHostEnvironment.WebRootFileProvider` so `IFileVersionProvider` covers the same file set (closing 33 with the same line). Relocation under `wwwroot/` was rejected because `ThemeProvider`, `CommonModelFactory`'s favicon *probe* and `PictureService`'s thumbnail *write* all require the files to stay under the content root. The allow-list is also what keeps `App_Data/Settings.txt` (the connection string) unreachable — `System.Web` blocked `App_Data` implicitly and ASP.NET Core does not.
    - **TWO DEFECTS FOUND AND FIXED.** (1) `Web.config` with a capital W silently loses the ANCM handler on any case-sensitive filesystem: the SDK's `TransformWebConfig` looks for the literal name `web.config`, so a Linux-built publish emitted a `Web.config` with **no `<aspNetCore>` element at all** and would have failed to start under IIS. (2) `.gitignore` ignored the whole `Content/Images/Thumbs/` directory, which had swallowed `placeholder.txt` — without it the directory is absent from a fresh clone and `PictureService.DeletePictureThumbs` throws `DirectoryNotFoundException`.
    - **CORRECTION to the deferral-39 framing:** it was written as "re-express the exclusions", but the larger half turned out to be **inclusions** — the SDK classifies `.css`/`.js`/`.png`/`.xml`/`.sql`/`.ttf`/`.mmdb` as `None` items, which default to `CopyToPublishDirectory=Never`, so a publish of the pre-7.4 project produced an app with **no static assets, no installation scripts, no browscap database and no PDF font**.
    - **VERIFIED, not just compiled:** two throwaway probes (both deleted, `git status` clean) asserted **74 facts, all PASS**, with a proven canary — 14 `NopConfig` properties bind, 6 `Authentication` values bind, the legacy `appSettings` keys behave exactly as in 3.90, the deferral-40 symptom reproduces before the fix, 8/8 asset paths serve and 19/19 non-asset paths refuse, a real `?v=<sha256>` is emitted, and 10 live HTTP requests confirm 404 for `Settings.txt`/`appsettings.json`/`web.config`/`*.cshtml`/`*.dll`. `dotnet publish` was audited against **planted secret files**.
    - _Requirements: 1.4, 4.6, 5.3, 5.8_
  - [x] 7.5 Remove obsolete Nop.Web files
    - Remove `AssemblyInfo`, `Global.asax`, `RouteConfig`/`*Config` App_Start files, and `Views/web.config` files rendered obsolete by the SDK/ASP.NET Core model
    - **RESULT: still 0 errors / 15 warnings** — the 7.3/7.4 baseline is preserved byte-for-byte (same 10 upstream `SYSLIB*` + 5 `CS0618`; no warning added). **Deleted:** `Views/Web.config`, `Themes/DefaultClean/Views/Web.config`, and the three dead IE8 assets (`Themes/DefaultClean/Content/css/ie8.css`, `Scripts/selectivizr.min.js`, `Scripts/respond.min.js`). **Deferral 18.5 CLOSED.** One deferral opened (7.5-1, per-theme `_ViewImports`). See runtime-deferrals.md §37–§39.
    - **`Properties/AssemblyInfo.cs` is KEPT, and the task text asking to remove it is WRONG for this solution — measured both ways.** `Directory.Build.props` sets `GenerateAssemblyInfo=false` for the whole solution (task 2.3's decision, so hand-kept files stay authoritative), which means the SDK emits **no replacement attributes**. Building with the file removed produced `AssemblyVersion 0.0.0.0` instead of `3.9.0.0`, and dropped `AssemblyTitle`/`AssemblyFileVersion`/`ComVisible`/`Guid` outright. It is also what all four migrated upstream projects do (task 6.5 §18.2 recorded the same decision for `Nop.Web.Framework`). Deleting it would require flipping a solution-wide property for one project and would make `Nop.Web` the only inconsistent one, with 26 projects still to migrate.
    - **NEW FILE — `Themes/DefaultClean/Views/_ViewImports.cshtml`, and it is NOT optional.** 7.3's note above says both `Views/Web.config` files were superseded by `Views/_ViewImports.cshtml`; **that is true of the first and false of the second.** Razor resolves `_ViewImports.cshtml` by walking up from the view's **own** directory, so the theme tree never sees the one under `Views/`. **Measured, not assumed:** a probe view at `Themes/DefaultClean/Views/Shared/` containing `@T("Account.Login")` failed with `CS0103: The name 'T' does not exist`; after adding the theme `_ViewImports.cshtml` the identical probe compiled at 0 errors. Both probes deleted, `git status` verified clean. Without it the deletion would have been a silent capability loss: `ThemeableViewLocationExpander` searches the theme tree **first** for every storefront view, so the next theme override to use `T()` — i.e. nearly any — would fail confusingly.
    - **Confirmed absent rather than assumed:** no `App_Start/` directory and **no `*Config.cs` file of any kind** exists under `Nop.Web` (3.90 put route/bundle registration in `Global.asax.cs`, not in App_Start), so that clause of the task text had nothing to act on. `Global.asax*` and `Web.Debug/Release.config` verified already gone.
    - **`FilePermissionHelper.GetFilesWrite()` fixed (deferral 18.5)** in the gated `Nop.Web.Framework`: the `~/Global.asax` entry is removed (task 7.2 deleted that file; the entry was harmless only because `CheckPermissions` swallows every failure and so reported a non-existent path as "permission OK"). `web.config` is **kept** — 7.4 retained it as the ANCM shim. Noted in passing: this list always said lowercase `web.config` while 3.90's file was `Web.config`, so the check silently missed the real file on Linux until 7.4's rename made it correct. **`Nop.Web.Framework` re-gated: 0 errors / 10 warnings**, its recorded 6.6 baseline, unchanged.
    - **Dead assets proven dead before deletion:** a repository-wide search (all file types, `.git` and `obj`/`bin` excluded) found the three IE8 files referenced **only** by their own copyright banners and 7.3's explanatory comment — no `AppendCssFileParts`/`AddScriptParts` call anywhere names them. Their sole loader was removed at 7.3 because `System.Web.HttpBrowserCapabilities` has no ASP.NET Core replacement, so they are obsolete by the platform, not by preference. `ie_warning.jpg` was checked and **kept** — still live via `Views/Shared/OldInternetExplorerWarning.cshtml`, which `_Root.cshtml` renders.
    - **`Nop.Web.csproj` cleanup:** the two interim `<Content Update="…Views\Web.config" CopyToPublishDirectory="Never" />` entries 7.4 added are **removed**, not left as no-ops, now that the files are gone; verified neither path appears in publish output.
    - **SCOPE REDUCED BY TASK 7.2:** `Global.asax` and `Global.asax.cs` are already deleted — they carried the `System.Web.Mvc` / `FluentValidation.Mvc` / `StackExchange.Profiling` references that blocked 7.2's own compile. See runtime-deferrals.md §20.
    - **SCOPE REDUCED BY TASK 7.4:** `Web.Debug.config` and `Web.Release.config` are already deleted, and `Web.config` was reduced to the IIS/ANCM hosting shim and renamed to lowercase `web.config` (do **not** delete it — it carries the two live IIS settings and is the file the SDK merges the ANCM handler into). See runtime-deferrals.md §33.6.
    - **ADDITIONS FROM TASK 7.3:** `Views/Web.config` and `Themes/DefaultClean/Views/Web.config` are now fully superseded by `Views/_ViewImports.cshtml` (both the `pageBaseType` and the `<namespaces>` list were translated) and can be deleted. Three static assets became unreferenced when the IE8 browser-capability branches were removed from `Themes/DefaultClean/Views/Shared/Head.cshtml`: `Themes/DefaultClean/Content/css/ie8.css`, `Scripts/selectivizr.min.js`, `Scripts/respond.min.js`. `FilePermissionHelper.GetFilesWrite()` still asks for write access to `~/Global.asax`, which no longer exists — see runtime-deferrals.md §18.5.
    - **NOTE FROM TASK 7.4 on those two `Views/Web.config` files:** they are already excluded from publish output (`CopyToPublishDirectory="Never"` in `Nop.Web.csproj`), because both declare a `System.Web.WebPages.Razor` `<configSections>` group and IIS parses every file literally named `web.config` in the served tree — publishing them would produce an **HTTP 500.19**. Once 7.5 deletes them, those two csproj entries become harmless no-ops and can be removed. `Themes/DefaultClean/theme.config` must **stay** — `ThemeProvider` reads it.
    - _Requirements: 1.5_
  - [x] 7.6 Clean-compile gate — Nop.Web
    - Build `src/Presentation/Nop.Web/Nop.Web.csproj -c Debug` using the containerized .NET 10 SDK command in `build-environment.md`; resolve to zero errors; verify no `System.Web*` references remain. Plugins remain blocked until the Nop.Admin gate (8.8) also passes
    - **GATE PASSED: 0 errors / 15 warnings** (`--no-incremental`, `obj`/`bin` removed first, containerized `mcr.microsoft.com/dotnet/sdk:10.0`). Warnings are the unchanged 10 upstream `SYSLIB0014/0021/0023/0045/0051` in `Nop.Core`/`Nop.Services` plus 5 `CS0618` on FluentValidation 7.x's obsolete `Custom(...)` in three `Validators/` files this task did not touch (design §9 pin). **No warning was added by 7.5.**
    - **No `System.Web*` in source: 0 real hits across 435 `.cs`/`.cshtml` files.** Scanned with a comment-blanking scanner (`@* *@`, `/* */`, `//`, string/verbatim-string aware) so explanatory prose cannot produce a false positive, and **the scanner was proven able to fail** by planting a `using System.Web.Mvc;` canary, which it caught. Tokens searched also included `HttpContext.Current`, `HttpContextBase`, `HttpPostedFileBase`, `MvcHtmlString`, `System.Configuration`, `System.Runtime.Caching`.
    - **No `System.Web*` in the emitted assembly: 0 banned entries in `Nop.Web.dll`'s 59-entry AssemblyRef table** (read via `System.Reflection.Metadata`, not text search). Also 0 for `ImageResizer`, `WebGrease`, `System.Web.Optimization`, `Autofac.Integration.*`, `MiniProfiler`, `StackExchange.Profiling`, `EntityFramework` (EF6), `Antlr`, `Microsoft.Web.*`. Assembly identity confirms `Nop.Web v3.9.0.0`, i.e. `AssemblyInfo.cs` is doing its job.
    - **One honest exception to report: `System.Drawing.Common` 4.7.2 IS in the output directory and `deps.json`** — pre-existing and deliberate, not a 7.5 regression. It is a **direct `PackageReference` of no project**; `project.assets.json` shows the single edge `EPPlus/4.5.3.3 → System.Drawing.Common`, and task 4.2 §10 pinned it to 4.7.2 to clear `NU1904`/CVE-2021-24112 (RCE via crafted metafile) which 4.7.0 carries. Nothing in the repository binds a type from it — `Nop.Services` only names `System.Drawing.Color`, which lives in the cross-platform `System.Drawing.Primitives`. `Nop.Web.dll` has **no** reference to it.
    - **No swallowed diagnostics** on a `-v:normal` log (900 lines): `"converted to a warning"` 0, `ContinueOnError` 0, `NU1901`–`NU1904` 0, `error MSB*` 0, and 0 Six Labors licence lines.
    - **All four upstream projects re-verified at 0 errors** after `obj`/`bin` removal: `Nop.Core` 0/3, `Nop.Data` 0/3, `Nop.Services` 0/10, `Nop.Web.Framework` 0/10 — each matching its recorded gate baseline.
    - **`dotnet publish` audited:** nothing deleted was load-bearing and 7.4's exclusions still hold. Present: `appsettings.json`, the transformed `web.config` (ANCM `AspNetCoreModuleV2` + `hostingModel="inprocess"` still merged, alongside 7.4's preserved `urlCompression`/`X-Powered-By`), `App_Data/browscap.xml`, `App_Data/Install`, `FreeSerif.ttf`, `Content/Images/Thumbs/placeholder.txt`, `Content/files/ExportImport/Index.htm`, `Scripts`, `theme.config`, `preview.jpg`, `styles.css`, `favicon.ico`, `ErrorPage.htm`, `FileNotFound.htm`. Absent: `App_Data/Settings.txt`, `InstalledPlugins.txt`, `browscap.crawlersonly.xml`, both `Views/Web.config`, `launchSettings.json`, the three IE8 assets, and **0** `.cs`/`.cshtml`/`.csproj` files. The only `web.config` in the publish tree is the root shim. The 195 compiled Razor view types include `Themes_DefaultClean_Views__ViewImports`, confirming the new theme file is compiled and applied.
    - _Requirements: 3.1, 3.2, 3.3, 4.2_
  - [x] 7.7 Smoke-check the ASP.NET Core host
    - Add a minimal integration/smoke check verifying the host starts, the Autofac container builds and core services resolve, one controller route responds, one Razor view renders, one former-module middleware executes, and `appsettings.json` binds (representative, non-gating)
    - **RESULT: the host runs, and the check found FIVE REAL DEFECTS — THREE of them release blockers that made nopCommerce IMPOSSIBLE TO INSTALL.** Two deferrals recorded as RESOLVED were **not**. Full analysis in runtime-deferrals.md §41–§44.
    - **NEW PROJECT: `src/Tests/Nop.Web.SmokeTests`** — `WebApplicationFactory<Nop.Web.Program>` + `Microsoft.AspNetCore.Mvc.Testing` 10.0.12 (the one new central pin) + NUnit 3.14.0, following task 4.5's pattern. **49 tests. 36 pass / 0 fail / 13 skip with a database installed; 32 pass / 0 fail / 17 skip without one.** It starts the REAL host: the real `Program.Main` through to `app.Run()`, the real Autofac container, the real middleware order, the real compiled Razor views — only `IServer` is swapped for `TestServer`. **No production source file was changed to enable testing** (`Program` was already a `public class` with a conventional `Main`, so the allowed `partial` accommodation was not needed).
    - **BLOCKER 1 — deferral 4.8 was marked RESOLVED and 7.2's fix was INERT.** `Program.InitializeDatabaseSchema()` opens with `if (!DataSettingsHelper.DatabaseIsInstalled()) return;` and runs once at startup, but the case 4.8 is about is *installing onto an empty database*, where at startup there is no `Settings.txt` — so it early-returned; on later starts the store is installed and the initializer short-circuits. Dead in both directions. Measured by POSTing the real installer form: **`Setup failed: Entity: Store State: Added … Invalid object name 'Store'.`** — 4.8's predicted symptom verbatim. Fixed in `Nop.Data/SqlServerDataProvider.InitDatabase()`, which is the installer's own call and where EF6's `Database.SetInitializer` hook fired.
    - **BLOCKER 2 — deferral 4.12's severity was wrong.** It said "not required for the compile gate", which is true and misleading: `App_Data/Install/SqlServer.StoredProcedures.sql` joins `Product_Id`/`ProductTag_Id`/`Customer_Id`/`CustomerRole_Id` **by name**, so stored-procedure creation failed with **`Invalid column name 'ProductTag_Id'`** and installation aborted. All **8** join tables now pin their 3.90 column names via `HasColumnName`, exactly as 4.12 recommended. Only the column name is pinned — no query or navigation code changed.
    - **BLOCKER 3 — NEW: EF Core's FK index naming collides with 15 of nopCommerce's own indexes.** `ForeignKeyIndexConvention` names them `IX_<Table>_<Column>`; EF6 used `IX_<Column>`. Measured: **`Setup failed: … index … 'IX_StateProvince_CountryId' already exists`**, and because the custom script aborts at the first failure **none of the 57 later indexes was created either**. Fixed by prefixing every `CREATE INDEX` in `SqlServer.Indexes.sql` with a guarded `DROP INDEX` — **drop-and-create, not `IF NOT EXISTS`**, because nopCommerce's definitions are richer (e.g. `IX_StateProvince_CountryId` carries `INCLUDE ([DisplayOrder])`) and skipping would have silently kept EF Core's narrower index.
    - **DEFECT 4 — NEW: eight paths break on any case-sensitive filesystem, i.e. on every Linux/container deployment.** Same class as task 7.4's `Web.config` → `web.config` rename, which was treated as a one-off; it was not. `PictureService` ×3 (`~/content/images[/thumbs]` vs `Content/Images[/Thumbs]`) — **the entire filesystem picture store was dead**; `CodeFirstInstallationService` ×4 (`~/content/samples/`) — **sample-data install failed**; `PdfService` ×1; and `Views/Install/Index.cshtml` ×3 — **the install page rendered with no stylesheet** (measured: `/Content/Install/style.css` → 302, `/Content/install/style.css` → 200). All corrected, then **two systematic audits written and run to close the class**: all 22 `MapPath("~/…")` literals and all 30 `~/Content|Scripts|Themes/…` view/stylesheet references now resolve case-exactly (0 mismatches). A permanent test walks every asset the install page emits and requires 200 from each.
    - **DEFECT 5 — deferral 7.2-1 resolved, and it had a second cause nobody had identified.** An unreachable database killed the host at startup (`NopException: No database instance`, exit 134). Making `InitializeDatabaseSchema()` non-fatal was not enough — it then died at `StartScheduledTasks → TaskManager.Initialize → ScheduleTaskService.GetAllTasks`, which reads the `ScheduleTask` table. Both are now guarded. **This is a reversal of 7.2's "deliberately not swallowed", and it is not 3.90 parity in 7.2's favour**: in System.Web a throw from `Application_Start` failed only the triggering request and was retried on the next one, so the worker survived and recovered by itself; an exception out of `Program.Main` terminates the process and produces a restart loop. Verified after the change: the host starts and stays up with the database down.
    - **VERIFIED BY EXECUTION — the six things the task names, plus tasks 7.3/7.4's prioritised list.** Host starts; **ONE** Autofac container (`EngineContext…Container` reference-equal to `app.Services.GetAutofacRoot()`); per-request scope genuinely shared (two `IWebHelper` resolves in one request are the same instance, and the same one `RequestServices` yields — deferral 1.3); plugin discovery **ran** (`ReferencedPlugins` non-null, count 0, which is correct — no plugin project is migrated yet); `appsettings.json` binds **as values off the Autofac singleton and off the live `CookieAuthenticationOptions`**, including `ExpireTimeSpan == 30 days`, the exact 30-minute silent fallback deferral 7.13 warned about; the install controller responds and its compiled Razor view renders; `InstallUrlMiddleware` redirects `/` and `/cart` and is proven to sit before routing. With a database: the **home page and its ~15 `@Html.Action` child actions all render** (deferral 7.3-1 — task 7.3 called this the single most important thing for 7.7 to do, and it had only ever been probed against a synthetic app); a real product slug resolves through `SlugRouteTransformer`; all seven `SuppressMatchingMetadata` routes still generate URLs against the **real** endpoint set; `/cart` is not swallowed by `{generic_se_name}`; empty widget zones render nothing and do not throw; **9 asset URLs carry a real `?v=<sha256>` and all return 200**; `IsSearchEngine()` is live (Googlebot true, Chrome false; cold 1171 ms, warm 19 ms, crawler file written); **`GET /App_Data/Settings.txt` returns 404 with a real connection string in it**; and **FluentValidation demonstrably REJECTS** invalid input on both the install and register forms (deferral 11.20, security-relevant — previously only inferred from a registration in `MvcOptions`).
    - **THE HARNESS WAS PROVEN ABLE TO FAIL** before its green run was believed: `HarnessCanaryTests` is an `[Explicit]` fixture with one deliberately-false assertion per mechanism the suite depends on (real HTTP through `TestServer`, real container resolve, probe-middleware output). Observed **3 failed / 0 passed**. This migration has twice been bitten by silently-swallowed failure, so a green run that had never been shown to go red would not have been reported as evidence.
    - **NO REGRESSION.** All gates re-verified with `--no-incremental` after `obj`/`bin` removal: `Nop.Core` **0**/3 · `Nop.Data` **0**/3 · `Nop.Services` **0**/10 · `Nop.Web.Framework` **0**/10 · `Nop.Web` **0**/15 — each exactly its recorded baseline, **no warning added**; `"converted to a warning"` 0 on all seven builds. `Nop.Tests` still 4 passed / 0 failed.
    - **FIVE NEW DEFERRALS: 7.7-1** a refused XSRF POST answers **404 instead of 400** (task 7.2's `UseStatusCodePagesWithReExecute` rewrites any bodiless 4xx; 7.2 judged this "uncommon", but it hits every `[PublicAntiForgery]` action — measured `POST /login` 200 vs `POST /register` and `POST /contactus` 404 — and will hit the whole admin surface at 8.3; **not** a security hole, the action does not run) — **✅ RESOLVED ahead of 8.3, see runtime-deferrals.md §45**; **7.7-2** the smoke project is not in `NopCommerce.sln` (task 18.1; it must NOT become a gate); **7.7-3** EF Core adds ~97 unrequested FK indexes (97 of 100 on a fresh database) — deliberately NOT decided here, since EF6 also created FK indexes under different names, so removing the convention would drop indexes rather than restore parity; **7.7-4** both case-sensitivity audits still need running over `Administration/` (tasks 8.4/8.5 — on this evidence they will find instances); **7.7-5** a scope note listing what is still unexercised — the entire admin UI, all 20 plugins, sign-in, checkout/payment/email, deferral 4.11's Fast-installer path, and Windows.
    - _Requirements: 4.1, 4.3, 4.4, 4.5, 4.6, 1.4_

- [ ] 8. Port Nop.Admin to ASP.NET Core
  - Largest single UI surface in the application: `src/Presentation/Nop.Web/Administration/Nop.Admin.csproj`, **277 `.cs` files** and **325 `.cshtml` Razor views**. References Nop.Core, Nop.Data, Nop.Services, Nop.Web.Framework — and neither references nor is referenced by Nop.Web, so this group is a **parallelizable sibling of task 7** once the 6.6 gate passes (design §6).
  - **Expect the highest iteration count of any stage in this plan.** Per design §6, run this group as a repeated `build → fix → build` loop against the containerized SDK rather than attempting a single pass; step 8.8 will be re-entered many times.
  - [x] 8.1 Convert Nop.Admin.csproj to SDK-style Web project at net10.0
    - Convert to `<Project Sdk="Microsoft.NET.Sdk.Web">` with `<TargetFramework>net10.0</TargetFramework>` (cross-platform, not `net10.0-windows`)
    - `packages.config` → `PackageReference`; delete `packages.config`; drop `Microsoft.AspNet.Mvc/Razor/WebPages` and `Autofac.Mvc5` in favour of ASP.NET Core MVC + `Autofac.Extensions.DependencyInjection`; add the `SixLabors.ImageSharp` `PackageReference` needed by 8.6
    - Drop the explicit `<Compile Include=.../>` and `<Content Include=.../>` globs (implicit globbing), plus `ProjectGuid`, `TargetFrameworkVersion`, Configuration/Platform blocks, `Import *.targets`, and BCL `<Reference Include="System.*"/>` entries including `System.Drawing`
    - Point `ProjectReference`s at the already-migrated `Nop.Core`, `Nop.Data`, `Nop.Services`, and `Nop.Web.Framework`
    - **FROM TASK 7.4 (new deferral 7.4-2, SECURITY-RELEVANT):** re-express the 3.90 publish exclusion for **`Administration/db_backups/*.bak`** here. `Nop.Web.csproj` structurally cannot do it — `Administration\**` is in its `DefaultItemExcludes`, so nothing under it is an item of that project — yet in 3.90 Nop.Admin dropped its output into `Nop.Web\bin`. Database backups must not reach a published, web-served directory. Use `<None Update="db_backups\**" CopyToPublishDirectory="Never" />`. Also note that 7.4 deliberately did **not** reproduce 3.90's `<mimeMap fileExtension=".bak">`, so backups are not downloadable as static files either. See runtime-deferrals.md §34.
    - **DONE. `<None Update="db_backups\**" CopyToPublishDirectory="Never" />` — the exact form prescribed above — is WRONG IN BOTH DIRECTIONS, and verifying rather than assuming is what caught it.** Too broad: it also withholds `db_backups\placeholder.txt`, and `MaintenanceService.GetAllBackupFiles()` **throws** `IOException("Backup directory not exists")` on an absent directory, so the admin Maintenance page would break — the same trap 7.4 hit with `Content\Images\Thumbs\placeholder.txt`. Not sufficient either: `placeholder.txt` is a `.txt`, hence a `None` item, hence `CopyToPublishDirectory=Never` **by default** — measured, the placeholder was absent from the first computed publish set with no exclusion touching it. The shipped form is an ordered pair: `db_backups\**` → `PreserveNewest` in an include group, then `db_backups\**\*.bak` → `Never`. Verified with a real `.bak` planted on disk: placeholder **present**, `.bak` **absent** — a discriminating result, where testing the exclusion alone cannot distinguish "the rule works" from "`None` defaults to `Never` anyway". Also note the larger half of the work was **inclusions**: the SDK classifies `.js`/`.css`/`.png`/… as `None`, so without added rules a published Nop.Admin has **no CSS, no JavaScript, no AdminLTE/bootstrap/Kendo/TinyMCE** at all. See runtime-deferrals.md §46.2.
    - **RESULT: 2959 unique `CS*`/`RZ*` errors across 673 files; restore clean, evaluation clean, zero `MSB*`/`NETSDK*`/`NU*`; resolved `Compile` 277 and `Content` 340 with ZERO leakage either way; all five upstream baselines and both test suites unchanged.** Three missing namespaces plus one Razor family account for everything: `System.Web.Mvc` (330 `CS0234` + most of 2687 `CS0246`), `System.Web.Routing` (30), `System.Web.Configuration` (2 — a **removal**, see 8.7), and Razor `@helper` (78 `RZ1002` in 26 views) + 4 `RZ2005`/4 `RZ1011` in two files. Full breakdown by directory and root cause in runtime-deferrals.md §46.3.
    - **FOUR NEW DEFERRALS. 8.1-4 is HIGH and should be read before starting 8.2:** the Razor source generator names views relative to **their own project root**, so the admin views will compile with identifiers `/Views/…`, which `ThemeableViewLocationExpander` does **not** search — it emits `/Administration/Views/…`. Measured both from the generator's output paths and from the built `Nop.Web.dll`'s 108 compiled identifiers. §14.30 flagged the opposite direction; this survives the expander being registered correctly. Three options, none decided. Also **8.1-1** `Content/Roxy_Fileman/tmp/` does not exist so the file manager's zip download throws (pre-existing in 3.90; 8.5/8.6); **8.1-2** two case-sensitivity defects in `Views/Shared/_AdminLayout.cshtml` (8.4); **8.1-3** `Server.MapPath` with a *relative* path in `RoxyFilemanController` has no ASP.NET Core equivalent (8.6). See runtime-deferrals.md §48.
    - **DEFERRAL 7.7-4's CASE-SENSITIVITY AUDITS WERE RUN HERE, not deferred to 8.4/8.5, because the finding list shapes those tasks.** 7.7's scripts were throwaway and uncommitted, so both were re-implemented to the same specification (comments stripped, resolved segment-by-segment against the real filesystem). Audit A — 5 `MapPath("~/…")` literals, **0** mismatches. Audit B — 116 filesystem-rooted `~/…` references, **2** mismatches, **both in `Views/Shared/_AdminLayout.cshtml`**, the layout every admin page uses: line 43 `~/Administration/scripts/…` should be `Scripts` (the admin navigation script, and it goes through `AppendScriptParts`, so `IFileVersionProvider` also silently emits no `?v=`), line 99 `~/administration/content/images/…` should be `Administration/Content/images`. A supplementary audit of 582 relative `url(...)` refs in the 87 admin stylesheets found **0**. Fixing the two is **8.4**'s. See runtime-deferrals.md §47.
    - **`Properties/AssemblyInfo.cs` is KEPT and step 8.7's instruction to remove it is WRONG for this solution**, for the reason task 7.5 measured for `Nop.Web` (runtime-deferrals.md §37.1): `Directory.Build.props` sets `GenerateAssemblyInfo=false` solution-wide, so the SDK emits no replacement attributes and deleting the file drops `AssemblyVersion` from `3.9.0.0` to `0.0.0.0`. See runtime-deferrals.md §46.4, which also corrects §7.18 — the ImageSharp licence diagnostic does **not** affect this project, because the pin is 2.1.13 (no `build/` targets), not 4.1.1.
    - _Requirements: 5.7, 1.1, 1.2, 1.3, 1.5, 2.4, 4.11, 5.10_
  - [x] 8.2 Replace the Admin AreaRegistration with ASP.NET Core area routing
    - Delete the legacy `AreaRegistration`-derived registration for the `Admin` area (`RegisterArea`/`AreaRegistrationContext.MapRoute`) — it has no ASP.NET Core equivalent
    - Apply `[Area("Admin")]` to admin controllers and register `MapAreaControllerRoute("areaAdmin", "Admin", "Admin/{controller=Home}/{action=Index}/{id?}")` through nopCommerce's `IRouteProvider` reimplemented over `IEndpointRouteBuilder`, preserving the existing `Admin` route prefix
    - Contribute the route registration from Nop.Admin for discovery by the Nop.Web host (no `Global.asax` declaration)
    - **FROM TASK 8.1 (new deferral 8.1-4, HIGH — read before starting):** the admin views will compile with Razor identifiers `/Views/…` (relative to *Nop.Admin's* own project root — measured from the source generator's output paths and from `Nop.Web.dll`'s 108 compiled identifiers), but `ThemeableViewLocationExpander` emits `/Administration/Views/…`, so **nothing resolves**. §14.30 flagged the opposite direction; this survives the expander being registered correctly. Three options — prefix the compiled identifiers, change the expander's two admin formats (watch the collision with the storefront's identical non-themed fallbacks, and it edits a gated project), or move the views to `Areas/Admin/Views/` — none decided. Whichever is chosen, §16.1's deliberately-preserved 3.90 ordering quirk (`…/Views/Shared/{0}` searched **before** `…/Views/{1}/{0}`) must survive. Also note this project has **no `Areas/` directory** today: the views are at `Administration/Views/`. See runtime-deferrals.md §48/8.1-4.
    - **ALSO FROM 8.1:** 33 distinct `~/Admin/…` literals in the admin views hard-code the `Admin` route prefix that `MapAreaControllerRoute` must preserve. They were catalogued by 8.1's audit (runtime-deferrals.md §47) but not touched — and the prefix **is** preserved, so they are still correct.
    - **DONE. 3.90's registration, read from the file rather than from this plan: `context.MapRoute("Admin_default", "Admin/{controller}/{action}/{id}", new { controller = "Home", action = "Index", area = "Admin", id = "" }, new[] { "Nop.Admin.Controllers" })`.** `AdminAreaRegistration.cs` is deleted; `Administration/Infrastructure/RouteProvider.cs` registers `MapAreaControllerRoute("Admin_default", "Admin", "Admin/{controller=Home}/{action=Index}/{id?}")` at `Priority = int.MaxValue` (3.90 ran `RegisterAllAreas()` before `RegisterRoutes`, and endpoint **order** beats precedence — §28.2). The route **name** is 3.90's `Admin_default`, not the `areaAdmin` this plan suggested. Two things could not transfer: `string[] namespaces` (no counterpart; controller discovery is application-part based) and `id = ""` → `{id?}`, which changes `RouteData.Values["id"]` from `""` to absent — verified harmless, nothing under `Administration/` reads it. `[Area("Admin")]` is declared **once**, on `BaseAdminController`, because `AreaAttribute : RouteValueAttribute` is `Inherited = true` and all 54 concrete controllers derive from it — **inheritance verified by execution**, not assumed. See runtime-deferrals.md §50.2.
    - **DEFERRAL 8.1-4 RESOLVED — option 3, on the user's approval: the 325-view tree was `git mv`d to `Administration/Areas/Admin/Views/`.** 326 renames, all tracked. **35 explicit `~/Administration/Views/…` view paths inside the admin views had to be rewritten** (`_ViewStart.cshtml`'s `_AdminLayout` plus 34 popup views' `_AdminPopupLayout`) — an explicit `~/` path bypasses location formats and is matched against the compiled identifier, so leaving them stale would have broken every admin page. In `ThemeableViewLocationExpander` the two dead `/Administration/Views/…` formats were removed and `AdminAreaPrefixLocationFormats` was renamed/repointed to `/Areas/{2}/Views/Shared/{0}` then `/Areas/{2}/Views/{1}/{0}` — **§16.1's Shared-before-controller quirk preserved deliberately**, since ASP.NET Core's own area formats order them the other way and that array is now the only thing making Shared win. `Nop.Web.Framework` re-gated at its exact **0 errors / 10 warnings** baseline. See runtime-deferrals.md §50.1 and the amendments to §14.30/§16.1.
    - **THE LOAD-BEARING UNKNOWN IS SOLVED, AND THE FIRST ANSWER WAS WRONG IN A WAY THAT WOULD HAVE KILLED THE STOREFRONT.** A `ProjectReference` from `Nop.Web` was rejected: it converts design §6's build/deploy relationship into a compile-time one and, decisively, MSBuild would build `Nop.Admin` (≈3000 errors) first, so `Nop.Web` would **stop building at all**. The shipped answer is three parts: (a) a `CopyNopAdminToHostOutput` post-build target in `Nop.Admin.csproj` reproducing 3.90's `OutputPath=..\bin\` drop, directionally and additively; (b) **a fix in `Nop.Core`** — `AppDomainTypeFinder.LoadMatchingAssemblies` used `AppDomain.Load(AssemblyName)`, which on .NET binds from the host's `deps.json`-derived TPA list and **does not probe the base directory**; a probe measured `FileNotFoundException` escaping `WebAppTypeFinder.GetAssemblies()`, i.e. dropping `Nop.Admin.dll` beside `Nop.Web.dll` would have **taken the host down at startup** from inside `NopHostedEngine.RegisterInto`. It now loads by path via `AssemblyLoadContext.Default.LoadFromAssemblyPath` and traces instead of throwing (`Nop.Core` re-gated at **0/3**); (c) new `Nop.Web.Framework/Infrastructure/NopApplicationPartExtensions.cs`, wired from `AddNopFramework`, contributing the type-finder's assemblies as MVC application parts **via `ApplicationPartFactory`** so compiled Razor views come along — which also fixed a latent half-closure of deferral **1.2**, since the old `AddPluginApplicationParts` added a bare `AssemblyPart` and therefore never contributed a plugin's views. See runtime-deferrals.md §50.3.
    - **`<OutputType>Library</OutputType>` added to `Nop.Admin.csproj` — required, and its absence would have failed the 8.8 gate.** `Microsoft.NET.Sdk.Web` defaults `OutputType` to `Exe` (measured via `-getProperty:OutputType`) and this project has no `Main`, so it would end with `CS5001`. Note it is **not observable today**: Roslyn never reaches entry-point resolution while ~3000 declaration-phase errors exist, so CS5001 would have appeared for the first time exactly when 8.3/8.4 finished. Both sides reproduced on the probe. See runtime-deferrals.md §50.5.
    - **VERIFICATION: 22 executed probe assertions (22 pass) + 6 new always-run smoke tests, both shown able to fail.** The probe (a throwaway `Microsoft.NET.Sdk.Web` stand-in for `Nop.Admin`, plus a host that deliberately does **not** reference it — deleted afterwards) proved: compiled identifiers are `/Areas/Admin/Views/…` with no `/Administration/` prefix; `[Area]` inheritance; the satellite becomes **both** `AssemblyPart` and `CompiledRazorAssemblyPart`; `FindView` picks the **Shared** view when both exist; `GET /Admin/ProbeThing/Both` → **200** rendering the Shared view inside the `_ViewStart` layout; and `GET /ProbeThing/Both` → **404**. Disabling the ordering fix flipped the rendered HTML from `[SHARED-BOTH]` to `[CONTROLLER-BOTH]` — the silent regression in observable form. `Nop.Web.SmokeTests` is **54 passed / 0 failed / 18 skipped** (was 48/0/18) and `HarnessCanaryTests` now **5 failed / 0 passed** (a fifth canary covers the expander mechanism). All five upstream baselines and `Nop.Tests` unchanged. See runtime-deferrals.md §50.4 and §50.6.
    - **THREE NEW DEFERRALS. 8.2-1 is HIGH and blocks group 10:** `PluginManager.PerformFileDeploy` loads shadow-copied plugin assemblies with the *same* `Assembly.Load(AssemblyName.GetAssemblyName(path))` call that §50.3 measured as broken — `~/Plugins/bin` is in no `deps.json` either — so the **entire plugin subsystem** will fail on the first migrated plugin. Unobserved only because no plugin exists yet (7.7 measured 0 plugins found). Also **8.2-2**: `dotnet publish` of `Nop.Web` does not include `Nop.Admin.dll`, so a published storefront silently has no admin area (8.5 / 18.x). Also **8.2-3**: 12 plugin view sites still reference `~/Administration/Views/Shared/_AdminPopupLayout.cshtml` / `_GridPagerMessages.cshtml`; full file/line list in runtime-deferrals.md §51.
    - **STILL INFERENCE, for 8.8:** that `Nop.Admin.dll` specifically loads and yields 325 identifiers under `/Areas/Admin/Views/`; that all 54 controllers really inherit the area value (the mechanism is proven on one); that the copy target fires in a real `Build` (it was exercised via `-t:` with a source file present, because `AfterTargets="Build"` cannot run while the build fails); that `GET /Admin/` reaches `HomeController.Index`. **And `Task_8_2_the_Admin_area_route_is_absent_until_Nop_Admin_compiles_KNOWN_GAP` MUST be inverted at 8.8**, or the suite is asserting the bug.
    - _Requirements: 4.10, 4.6, 4.7_
  - [x] 8.3 Convert all Nop.Admin controllers to ASP.NET Core MVC
    - `System.Web.Mvc.Controller` → `Microsoft.AspNetCore.Mvc.Controller` across every admin controller; map `ActionResult`/`ViewResult`/`RedirectResult`; port model binding, `TryUpdateModel`, and `ModelState` usages
    - Port grid/AJAX actions returning `JsonResult` to the ASP.NET Core `JsonResult`, supplying explicit serializer settings where the differing semantics change payload shape
    - Convert `[ChildActionOnly]` actions plus their `Html.Action(...)` call sites to **View Components** (note: `[ChildActionOnly]` no longer exists as a type — see the `[NopChildActionOnly]` entry below, which is the mechanism that restores its "not URL-reachable" property for actions that stay as actions)
    - Port admin permission filters and `AuthorizeAttribute` usages to ASP.NET Core filters with **policy-based authorization**
    - **FROM TASK 7.4 (deferral 7.3-4) — ✅ THE MECHANISM NOW EXISTS; 8.3'S REMAINING JOB IS ONLY TO APPLY THE MARKER.** Implemented ahead of this task, in `Nop.Web.Framework`, precisely so the admin surface ports onto finished infrastructure instead of needing a second pass over 277 controllers. Delivered: `Nop.Web.Framework/Mvc/NopChildActionOnlyAttribute.cs` (inert marker) + `Nop.Web.Framework/Mvc/NopChildActionOnlyConvention.cs` (an `IActionModelConvention` adding `SuppressMatchingMetadata` to every selector of a marked action), registered in `NopServiceCollectionExtensions.AddNopFramework`. The marker is already applied to all **48** `Nop.Web` actions. **What 8.3 must do: apply `[NopChildActionOnly]` to the admin equivalents — nothing else.** Do not re-author the mechanism, and do not register the convention again. Identify the admin actions the same way 7.3-4's fix did: `git grep -n ChildActionOnly 9cb503f -- 'src/Presentation/Nop.Web/Administration/*'` on the pre-migration tree, never by guessing — marking an action that was never `[ChildActionOnly]` deletes a legitimate URL endpoint. Two properties are already proven by execution and are worth knowing: suppression is scoped to the **action**, so it covers explicit `MapControllerRoute` endpoints as well as the `Default` route; and it does **not** hide the action from `IActionDescriptorCollectionProvider`, so the `Html.Action` bridge still resolves it. `Nop.Web.SmokeTests` has parameterised tests for all three invariants (`Deferral_7_3_4_*`) — add the admin cases to them. See runtime-deferrals.md §45.
    - **ALSO FROM 7.3 (deferral 7.3-1):** `Nop.Web/Extensions/ChildActionExtensions.cs` (the `Html.Action` bridge) is needed by the admin views too. Recommend **promoting it to `Nop.Web.Framework`** rather than duplicating it; the plugins then get it for free. **Task 7.7 verified the bridge works against nopCommerce's real controllers** (the home page's ~15 child actions all render), so promotion is a move, not a rewrite.
    - **FROM TASK 7.7 (deferral 7.7-1) — ✅ ALREADY FIXED, NOTHING FOR 8.3 TO DO.** A refused XSRF POST used to answer 404 instead of 400, because `PublicAntiForgeryAttribute`/`AdminAntiForgeryAttribute` return a **bodiless** `BadRequestResult` and `Program.cs`'s `UseStatusCodePagesWithReExecute("/page-not-found")` fired for *any* bodiless 4xx. Since `[AdminAntiForgery]` is applied across the whole admin surface, this was fixed ahead of this task rather than left to bite it: `Program.cs` now calls `Nop.Web.Framework.Infrastructure.NopStatusCodePagesExtensions.UseNopStatusCodePages()`, which re-executes for a 404 and only for a 404. Verified by execution — a bodiless 405 now survives where it previously became a 302/404, and a genuine 404 still re-executes to `PageNotFound`. The admin host, when 8.2/8.3 wire it, must use `UseNopStatusCodePages()` and not the stock middleware. See runtime-deferrals.md §45.
    - _Requirements: 4.8, 4.7, 4.2, 4.3, 4.5_
    - **✅ DONE — `Nop.Admin` went from 2953 unique `CS*`/`RZ*` diagnostics across 672 files to 590 across 505, and the number that matters: `Controllers/` 1651 → **0**, `Models/` 706 → **0**, `Helpers/` 8 → **0**, `Extensions/` 4 → **0**. `Validators/` and `Infrastructure/` were not touched. EVERY one of the 590 residuals is inside a Razor view or the generator's `*_cshtml.g.cs` output for one, and **every residual `CS0246` names a `*Model` type** — i.e. the single `_ViewImports.cshtml` cluster 8.1 predicted, wholly owned by **8.4**. Residual attribution: 504 `CS0246` + 78 `RZ1002` (`@helper`) + 4 `RZ2005` + 4 `RZ1011` → **8.4**; `System.Drawing`/`ColorTranslator` and the two relative `Server.MapPath` sites → **8.6** (they do not error today); static assets → **8.5**; `Web.config`/`AssemblyInfo` → **8.7**. See runtime-deferrals.md §53–58.**
    - **VERIFICATION — a probe that binds METHOD BODIES, and the masking measured both ways.** 590 declaration errors remain in the Razor-generated code, so a `Nop.Admin.csproj` build never binds a single controller body: a green "Controllers: 0 errors" reading proves nothing on its own. A throwaway `Microsoft.NET.Sdk` (deliberately **not** `.Web`) probe at `src/.probe83` compiled the same 277 `.cs` files with no Razor — no stubs needed — and its **first run reported 71 body-level errors in 21 files that the main build could not see**. Proven able to fail: a planted `DeliberateProbe83CanaryDoesNotExist()` was reported by the probe (71→72) and produced **0** occurrences in the `Nop.Admin` build. Final probe result **0 errors**; canary reverted, probe deleted, `git status` clean. Also worth knowing for future tasks: one mangled string literal in `RoxyFilemanController.cs` produced 6 `CS1xxx` and **suppressed all 584 other errors in the project** — an implausibly low count should be checked for parse failures before it is believed.
    - **BASELINES ALL EXACT, nothing regressed:** `Nop.Core` 0/3 · `Nop.Data` 0/3 · `Nop.Services` 0/10 · `Nop.Web.Framework` 0/10 · `Nop.Web` 0/15 (all `--no-incremental` after `rm -rf obj bin`), `Nop.Tests` 4 passed, `Nop.Web.SmokeTests` **54 passed / 0 failed / 18 skipped**, `HarnessCanaryTests` still **5 failed / 0 passed**. Swallowed-diagnostics check on the admin build: `"converted to a warning"` 0, `ContinueOnError` 0, `NU1901`–`NU1904` 0, Six Labors licence lines 0, `error MSB*` 0.
    - **DEFERRAL 7.3-1 RESOLVED — the `Html.Action` bridge is PROMOTED, not duplicated.** `git mv` of `Nop.Web/Extensions/ChildActionExtensions.cs` → **`Nop.Web.Framework/ChildActionExtensions.cs`**, namespace `Nop.Web.Extensions` → **`Nop.Web.Framework`**. The namespace choice is load-bearing: every `_ViewImports.cshtml` already carries `@using Nop.Web.Framework`, so **zero view call sites changed** — and 8.4's `Areas/Admin/Views/_ViewImports.cshtml` gets it for free, as do all 20 plugin projects. Body unchanged; both gated projects re-gated and both suites re-run before proceeding.
    - **DEFERRAL 7.3-4 (admin half) RESOLVED — 16 actions marked, identified from git history.** `[ChildActionOnly]` → `[NopChildActionOnly]`. `git grep -c ChildActionOnly 9cb503f` returns **16** across `Affiliate`(1) `Common`(4) `Customer`(2) `Home`(2) `Order`(4) `Setting`(2) `Widget`(1), and the working tree had exactly the same 16 before and after — so this is provably a pure in-place substitution with no set change, never a guess. 8.1's "32 sites" is the same 16 double-printed. The mechanism was not touched and the convention was not re-registered.
    - **DEFERRAL 18.4 (admin half) RESOLVED** — both `CommonController` ACL loops are now inside `if (OperatingSystem.IsWindows())`. A real correction, not warning suppression: `WindowsIdentity.GetCurrent()` sits **outside** `CheckPermissions`'s swallowing `try/catch`, so the admin System Info page **threw** on Linux.
    - **THE AUTHORIZATION PORT: THERE WAS NOTHING LEFT TO DO, and that is a measured finding.** `[AdminAuthorize]` on `BaseAdminController` is the ONLY authorization attribute anywhere under `Administration/` — no admin controller or action carries `[Authorize]`, a custom `AuthorizeAttribute`, an `OnAuthorization` override or an `IAuthorizationFilter`. All five class-level filters were ported by 6.2 and are unchanged. `AdminAuthorizeAttribute`'s recorded reason for **not** being a policy-based `[Authorize]` still holds and is now repeated on `BaseAdminController` so it is not re-litigated: it performs no principal/role test at all — it queries the database-backed `IPermissionService` keyed off `IWorkContext.CurrentCustomer`, a named policy would have to be registered by the HOST (which a class library cannot do), and it would fail **open** if a host forgot.
    - **`BaseAdminController`: four MVC 5 lifecycle members with no counterpart.** `Initialize(RequestContext)` → `override OnActionExecuting` (no `Initialize`, no `RequestContext`, `ControllerContext` is property-injected; `Controller` already implements `IActionFilter`). `IsAdmin` is consequently set after the authorization filters instead of before — **checked, not assumed:** none of the three admin authorization filters reads `IWorkContext.IsAdmin`. `AccessDeniedView()`'s `Request.RawUrl` → `GetEncodedPathAndQuery()`. **`OnException` REMOVED**: ASP.NET Core's `Controller` implements `IActionFilter`/`IAsyncActionFilter`/`IResultFilter` but **not** `IExceptionFilter`, and there is no controller-as-exception-filter plumbing — implementing it would compile and never run, a silent no-op. Nothing is lost and a 3.90 defect is fixed: 3.90's override logged and did not mark handled, so `Application_Error` logged it **again** — two `Log` rows per admin exception; task 7.2's `NopErrorLoggingMiddleware` now logs it once.
    - **JSON: the recorded decisions were FOLLOWED, not revisited.** The MVC 5 four-argument `Json` override collapses onto `override Json(object)` — which is `virtual` and, as in MVC 5, the overload every other funnels through, so **every `return Json(x)` in the 54 controllers still goes through `IsoDateTimeConverter`** with no per-call-site change. `ConverterJsonResult` stays on explicit Newtonsoft (6.2 §11.23) and `PropertyNamingPolicy = null` stays (6.4) — Kendo grids and `DataSourceResult` need PascalCase. Dropped as no-ops: `MaxJsonLength = int.MaxValue` (raised `JavaScriptSerializer`'s 4 MB cap; neither System.Text.Json nor Newtonsoft has a cap), `contentType`, `contentEncoding` (no `Response.ContentEncoding` exists), and **32 `JsonRequestBehavior.AllowGet` arguments** (already recorded as a relaxation at 6.2 §11.23).
    - **THE UPLOAD TRUNCATION BUG, FOUND FOR THE FOURTH AND FIFTH TIME.** `PictureController` and `DownloadController` both did `new byte[stream.Length]` + a single `Read()`. Wrong twice on ASP.NET Core: `Request.Body` is **not seekable** so `.Length` **throws**, and one `Read` need not fill the buffer. Both → `CopyTo` over a `MemoryStream`. Same bug 4.2 fixed in `Nop.Services` and 7.3 fixed in `Nop.Web`. `CommonController.GetBuildDate`'s `Read(buffer, 0, 2048)` over a PE header → `ReadAtLeast(..., throwOnEndOfStream: false)` for the same reason. Only visible because the probe bound bodies.
    - **`[Bind(Exclude = "…")]`, 16 sites — ASP.NET Core's `BindAttribute` has `Include` but NO `Exclude`** (the blacklist form was dropped from the platform). Each site drops the attribute and **resets the excluded member as the first statement of the body** (`Id`→0 ×11, `ConfigurationRouteValues`→null ×5, `CreatedOn`→default ×1). Verified for all 16 that no action reads the member. This is strictly better than simply deleting the attribute (what upstream 4.x did): the `Id` group does `model.ToEntity(new X())`, AutoMapper maps `Id`, and letting a client dictate a primary key on insert is a behaviour change.
    - **SECURITY-RELEVANT RELAXATION, RECORDED PROMINENTLY: 395 `[AllowHtml]` and 24 `[ValidateInput(false)]` attributes DELETED — four times `Nop.Web`'s 99/42 (deferral 7.3-3).** Both existed only to opt *out* of ASP.NET **request validation**, which does not exist in ASP.NET Core and has no successor, so **this cannot be restored**. Net effect: 3.90 screened HTML-looking input on every admin model property except the 395 marked; now every property behaves as if marked. Accepted because request validation was always defence in depth (the primary control is Razor's default output encoding) and because nopCommerce already disabled it wherever it mattered — the 24 `[ValidateInput(false)]` actions are the rich-text ones. What reviewers should know: previously-*marked* fields are unchanged in risk; previously-*screened* fields are no longer screened, and `@Html.Raw(...)` sites are where that matters. Rated Low–Medium as 7.3-3 was, because admin access already requires `AccessAdminPanel`.
    - **`IFormCollection` yields `StringValues`, 20 error sites, two shapes** — `var x = form[k]` → `string x = form[k]` (28 sites, a superset) and 4 inline `form[k] != null ? form[k].Split(...)` get a named `string` local. The implicit conversion is behaviourally identical to `NameValueCollection`'s indexer (single value as-is, several joined with `","`, missing key null). `.AllKeys` → `.Keys`, 14 sites. `Request.Files` → a new guarded `BaseAdminController.GetRequestFiles()` (12), `Request["x"]` → `GetRequestValue("x")` (4), `IFormFile.InputStream` → `OpenReadStream()` (8), `.ContentLength` → `.Length` (7), `.SaveAs` → `CopyTo` (1). Both helpers are `HasFormContentType`-guarded because ASP.NET Core **throws** on `Request.Form` for a non-form request where System.Web returned an empty collection.
    - **`HttpServerUtility` is gone entirely.** `Server.HtmlEncode`→`WebUtility.HtmlEncode` (7), `Server.UrlEncode`→`WebUtility.UrlEncode` (2), and **`Server.ScriptTimeout = 300` REMOVED at 4 sites** — ASP.NET Core/Kestrel impose no per-request execution timeout at all, so language import/export and setting import now run **untimed** (strictly more permissive); the modern equivalents are host configuration, not a per-action call. `[AcceptVerbs(HttpVerbs.Get)]`→`[HttpGet]` (5). `Request.PhysicalApplicationPath`→`CommonHelper.MapPath("~/")` — **and the path casing corrected in the same edit** (`"content\\files\\exportimport"` → `Content/files/ExportImport` via `Path.Combine`, the defect class 7.7 found 8 of). `Request.Url.IsLoopback` → a new `IsLoopbackRequest()` over `Connection.LocalIpAddress`, which is **more** accurate (`Uri.IsLoopback` tested the host string). `RouteTable.Routes.ClearSeoFriendlyUrlsCachedValueForRoutes()` → `LocalizedRoute.ClearSeoFriendlyUrlsCachedValue()` (§17.4b's named replacement, a deliberate no-op, kept so the intent stays visible). System Info reports trust level **"Full"** unconditionally per §2, and lists **request headers** where `ServerVariables` used to be — the honest subset, since System.Web's collection held IIS/ISAPI values with no protocol equivalent. `RoxyFilemanController`'s whole HTTP surface ported (`Response.Write`→`WriteAsync`, `TransmitFile`→`SendFileAsync` **with the temp-zip delete moved after the send**, `Response.End()` removed, `OutputStream`→`Body`, session object-indexer→`ISession.GetString`); its and `CommonController`'s `HttpContextBase` constructor parameters are gone — breaking, but both are registered reflectively.
    - **RESIDUAL `System.Web` SCAN: 276 `.cs` files, 0 real hits**, using a scanner that blanks `//`, `/* */` and `@* *@` (tracking string/char/verbatim literals) before searching 40 tokens — necessary because this task added a great deal of explanatory prose naming the legacy types. **Proven able to fail:** a planted `_ScanCanary.cs` with `using System.Web.Mvc;` was reported, then removed. `ChildActionOnly` inside `NopChildActionOnly` is excluded by an explicit allow-list, not by luck.
    - **THREE NEW DEFERRALS. 8.3-2 is bookkeeping that 8.8 MUST NOT SKIP:** the admin `[NopChildActionOnly]` and area-route smoke assertions cannot be written honestly today, because those tests read the real host's endpoint table and `Nop.Admin.dll` is not loadable until 8.4 finishes — adding a case now would fail or, worse, be written to skip, which is how a suite ends up asserting the bug. 8.8 must in one pass invert `Task_8_2_the_Admin_area_route_is_absent_until_Nop_Admin_compiles_KNOWN_GAP`, add admin cases to all three `Deferral_7_3_4_*` invariants (choosing at least one marked action whose name is shared with an unmarked one), and assert `GET /Admin/Common/BackupFileDownload` requires authorization. Also **8.3-1**: the System Info `<machineKey>` warning is REMOVED with nothing in its place — `MachineKeySection` does not exist on .NET and `<machineKey>` has no successor *setting* (Data Protection's key ring is configured in code), so `appsettings.json` must NOT grow a `machineKey` key; the underlying multi-instance risk is already recorded under 7.13, and surfacing it properly (interrogating `IXmlRepository`) is a new diagnostic, not a port — 8.7's decision. Also **8.3-3**: `RoxyFilemanController.MapPath` now **throws `NopException` naming deferral 8.1-3** for a relative path rather than guessing a target; not a regression, since all three callers are already inoperative (the `"../Uploads"` fallback is unreached because `conf.json` sets `FILES_ROOT`, `"../tmp/"` already fails per 8.1-1, and `VerifyAction`'s call site is commented out) — 8.6 must establish the *intended* target, not translate the `..`.
    - **THE `.bak` DOWNLOAD LINK IS REPLACED, AND 3.90 HAD A REAL SECURITY DEFECT THERE.** `CommonController.BackupFiles`'s grid link was `GetStoreLocation() + "Administration/db_backups/" + name`, i.e. backups served as **static files** — which never entered the MVC pipeline and therefore never met `[AdminAuthorize]`, so anyone who could guess a filename could download the whole database unauthenticated. It is now `Url.Action("BackupFileDownload", …)` pointing at a new action that inherits the class-level admin filters, re-checks `ManageMaintenance`, refuses any name containing a separator/`..`/invalid char, and confirms the canonicalised path is still inside the backup directory before `PhysicalFile`. The mimeMap was **not** re-added and the static-file allow-list was **not** widened.

  - [x] 8.4 Port all Nop.Admin Razor views to the ASP.NET Core Razor engine
    - **✅ DONE — `Nop.Admin` went from 590 unique `CS*`/`RZ*` diagnostics across 505 files to ZERO, so the 8.8 gate criterion is met.** `Nop.Admin`'s own warnings are **6**, and **not one was introduced by this task**: 4 `CS0618` on FluentValidation 7.x's obsolete `Custom(...)` in `Validators/` (design §9's deliberate pin), 1 `CS0618` `TimeZone.CurrentTimeZone`, 1 `SYSLIB0014` `WebRequest.Create` — all pre-existing 3.90 code that 8.3 predicted would surface once the project compiled, **owned by 8.7**. `git diff` confirms `Validators/`, `Models/`, `Controllers/`, `Extensions/`, `Helpers/` and `Infrastructure/` were untouched (0 files). **260 `MVC1000` cleared** (`Html.Partial` → `await Html.PartialAsync`, 260 sites in 219 files; zero `RenderPartial` sites existed). All five upstream baselines re-gated exact after `rm -rf obj bin`: `Nop.Core` 0/3 · `Nop.Data` 0/3 · `Nop.Services` 0/10 · `Nop.Web.Framework` 0/10 · `Nop.Web` 0/15. `Nop.Tests` 4/0; `Nop.Web.SmokeTests` 54 passed / 0 failed / 18 skipped; `HarnessCanaryTests` still 5 failed / 0 passed. See runtime-deferrals.md §59–62.**
    - **THE `_ViewImports.cshtml` LEVER, MEASURED: `Areas/Admin/Views/_ViewImports.cshtml` ALONE took the project from 590 errors to 326**, and the `CS0246` `*Model` family from **504 to zero**. The headline fell by less than 504 because Roslyn only binds a view's body once its declarations resolve, so ~213 body-level `CS0103` errors became visible in the same build — 7.3's §27 phenomenon. Two constraints were checked, not assumed: `System.Web.Optimization` is absent (and was never in the legacy admin `<namespaces>` list at all), and **all 29 `Nop.Admin.Models.*` namespaces are safe to import globally** — 188 namespace-level type names, **zero** collisions; the six that look duplicated (`OrderModel`, `ProductModel`, `StoreModel`, `ActivityLogModel`, `ProductAttributeModel`, `ProductAttributeValueModel`) are all **nested** types, which a `using` does not bring into scope. `Ajax.` occurrences: **0** across all 325 views, so `System.Web.Mvc.Ajax` needed no replacement.
    - **THE 78 `@helper` CONVERSIONS NEEDED A NEW SEAM, AND THE NAIVE ANSWER DOES NOT COMPILE.** 7.3's `void`-method-in-`@functions` pattern works when the helper is invoked as a **statement**; all 78 admin helpers pass their result as an **argument** to `RenderBootstrapTabContent`, whose parameter is `HelperResult`. Templated Razor delegates were rejected (they declare a *local*, and all 78 helpers sit at the bottom of their file while every call site is near the top — plus 13 files nest a `<text>` inside a helper body); partial views and a begin/end split were rejected too. So **`WebViewPage<TModel>.Capture(Func<Task>)`** was added, wrapping the method in a `HelperResult` that `PushWriter`/`PopWriter`s around it. **The methods are `async Task`, not `void` — measured:** the bodies contain `~/`-rooted attribute values, which Razor lowers into the URL-resolution tag helper, and a `void` conversion produced 34 `MVC1006` + 74 `CS4033`. Call sites: `Capture(TabX)` ×77, and `@Capture(() => RenderMenuItem(item, supportRtl))` ×2 in `Shared/Menu.cshtml` (the `@{ … }` form is rejected inside a `@foreach` body with `RZ1010` — measured). The scripted transformation's structural invariants were verified first across all 26 files: **0 anomalies**; bodies are byte-identical apart from being wrapped.
    - **THE CAPTURE MECHANISM WAS VERIFIED BY EXECUTION, AND THE PROBE WAS PROVEN ABLE TO FAIL.** A clean compile says nothing about *where* captured markup lands, and getting it wrong is silent — it compiles, passes the gate, and shows only in the HTML. A throwaway probe (deleted, `git status` clean) hand-wrote the generated shape and asserted 10 properties, all PASS, rendering `[before]<div class="tab-pane active" id="tab-info">[body-1][body-2]<span>[nested]</span>[body-3]</div>[after]`. With `PushWriter`/`PopWriter` commented out it reported **2 FAILED** and rendered every tab body **above** its wrapper with **every `tab-pane` div empty** — exactly what a naive `void` conversion would have shipped.
    - **`ViewCompatibilityExtensions` PROMOTED to `Nop.Web.Framework`** (`git mv`, namespace `Nop.Web.Extensions` → `Nop.Web.Framework`), same move/reason/mechanism 8.3 used for `ChildActionExtensions`: `Nop.Admin` needs all the shims and does not reference `Nop.Web`. The namespace choice means **no view call site in either project changed**. **New shim: `GetFullHtmlFieldId(this TemplateInfo, string)`** — ASP.NET Core kept `GetFullHtmlFieldName` but dropped `GetFullHtmlFieldId`, and the 15 call sites are all admin editor templates that need the id to attach a Kendo widget/TinyMCE to the input they just rendered. Written with MVC 5's exact name and signature so those 15 sites are byte-identical, and **verified by execution**: `"Locales[0].Name"` → `Locales_0__Name`, exactly what 6.3 measured MVC 5 produced.
    - Other substitutions, all reusing 7.3 §30: `HttpUtility.JavaScriptStringEncode` → `JavaScriptHelper.Encode` (19) · `HttpUtility.UrlEncode` → `WebUtility.UrlEncode` (3) · `HttpContext.Current.Request.RawUrl` → `Context.Request.GetEncodedPathAndQuery()` (6) · `new ViewDataDictionary()` → `Html.NewViewData()` (4, one of them a collection initializer that had to become an indexer assignment) · `@attribute.Foo` → `@(attribute.Foo)` (4, clearing all 8 `RZ2005`/`RZ1011`, in exactly the two files 8.3 named) · `Html.BeginForm(…, FormMethod, htmlAttributes)` → the 6-arg overload with `antiforgery: null` (4). **`Html.Action` was NOT converted to view components** — the promoted bridge handles all 69 sites unchanged. **The `TagBuilder.ToString()` trap does not arise here and that was checked:** zero `" + Html.…`, `Html.…() + "`, `string.Format(… Html.…)` or `Html.…().ToString()` sites; the four admin `StringBuilder`s append only plain strings.
    - **`Areas/Admin/Views/Web.config` DELETED**, and its now-dead `<Content Update="…" CopyToPublishDirectory="Never" />` entry **removed** from `Nop.Admin.csproj` rather than left as a no-op — as 7.5 did for `Nop.Web`'s two equivalents. There is no other view `Web.config` under `Areas/` (verified). `_ViewStart.cshtml` needed no change beyond 8.2's relocation. Note there is **no `Shared/DisplayTemplates/` folder** in this project — only `EditorTemplates/` (14 files).
    - **DEFERRAL 8.1-2 RESOLVED** — both casing defects in `Areas/Admin/Views/Shared/_AdminLayout.cshtml` fixed (`~/Administration/scripts/…` → `Scripts`; `~/administration/content/images/…` → `Administration/Content/images`), which also removes the blocker 8.5 was warned about. **The admin-VIEW half of deferral 7.7-4 is closed:** 81 filesystem-rooted `~/…` references → 0 mismatches; the `{0}`-substituted `kendo/{0}/…` family resolved concretely against `kendoVersion = "2014.1.318"` (all 4 files present, case-exact); `~/Content/Images/flags/…` confirmed correct as 8.1 stated. **The auditor was proven able to fail** with a planted mismatch, and correctly ignored the same path inside an `@* *@` comment. The 87 admin stylesheets remain **8.5**'s.
    - **The two dead bundling checkboxes REMOVED** from `Areas/Admin/Views/Setting/GeneralCommon.cshtml` (design §8). The model properties and `SettingController`'s read/write are deliberately **kept**; the one observable consequence is stated rather than glossed — the form no longer posts these fields, so a store that had bundling ON is written back OFF on the next save, which is harmless because the values are inert. **MiniProfiler: there were NO admin checkboxes to remove** — 7.3 flagged them as 8.4's, and a case-insensitive search across every `.cshtml`/`.cs` under `Administration/` returns **zero** hits; `GeneralCommonSettingsModel` has no `DisplayMiniProfiler*` property. Recorded so "found nothing" is distinguishable from "did not look".
    - **No `System.Web*` survives in the view tree: 326 files scanned, 0 real hits**, using a comment-blanking scanner (`//`, `/* */`, `@* *@`, with string-literal tracking) over 46 tokens — the same specification as 8.3's `.cs` scan. **Proven able to fail** with a planted `@using System.Web.Mvc` + `@HttpContext.Current`, while correctly ignoring an adjacent comment mentioning both. `Shared/Menu.cshtml`'s `siteMap.LoadFrom("~/Administration/sitemap.config")` verified still to work — it is a **physical** path through `CommonHelper.MapPath`, unaffected by 8.2's view relocation.
    - **NEW DEFERRAL 8.4-1 — 8.8 MUST FIX THIS FIRST OR EVERY ADMIN SMOKE ASSERTION IS VACUOUS.** `Nop.Admin` now compiles and `CopyNopAdminToHostOutput` does drop `Nop.Admin.dll` into `Nop.Web/bin/Debug/net10.0/` (verified present, 5.8 MB) — yet `Task_8_2_the_Admin_area_route_is_absent_until_Nop_Admin_compiles_KNOWN_GAP` **still passes**. That is not an 8.2 wiring failure: `WebAppTypeFinder` scans `AppDomain.CurrentDomain.BaseDirectory`, which under `dotnet test` is the **test project's** output directory, and `Nop.Admin.dll` is not copied there because the test project references `Nop.Web` and `Nop.Web` does not reference `Nop.Admin`. So inverting that test would fail, and all three `Deferral_7_3_4_*` admin cases plus the `BackupFileDownload` authorization assertion would be vacuous. This is the **third** instance of one root cause — §50.3's `AppDomainTypeFinder` fix and deferral 8.2-1's `PluginManager` are the others: .NET does not probe the base directory. See runtime-deferrals.md §60.
    - _(the original task text follows, kept for the record)_
    - **FROM TASK 8.3 — 8.4 NOW OWNS EVERY REMAINING ERROR IN THIS PROJECT, AND THE ATTRIBUTION IS EXACT.** After 8.3, `Nop.Admin` is at **590** unique diagnostics: **504 `CS0246`, every single one of which names a `*Model` type**, plus **78 `RZ1002`** (`@helper`, in 26 view files), **4 `RZ2005`** and **4 `RZ1011`** (in exactly `Areas/Admin/Views/Customer/_CustomerAttributes.cshtml` and `Areas/Admin/Views/Shared/_AddressAttributes.cshtml`). `Controllers/`, `Models/`, `Extensions/`, `Helpers/`, `Validators/` and `Infrastructure/` are all at **zero**, and the models compile fine — they are unresolvable *only inside views*. So the `CS0246` cluster really is one missing file, `Areas/Admin/Views/_ViewImports.cshtml`, exactly as 8.1 predicted; create it FIRST. The `RZ1002`/`RZ2005`/`RZ1011` families are the same two problems 7.3 solved in `Nop.Web` (§30): a Razor-v2 `helper` declaration becomes a `void` method in `@functions` (markup inside is written straight to the page output, so the bodies are unchanged; call it as `Name(x);` in a code context and `@{ Name(x); }` in a markup context, or you get `RZ1010`), and `@attribute.Foo` collides with ASP.NET Core 3.0's reserved `@attribute` directive so it needs `@(attribute.Foo)`.
    - **ALSO FROM 8.3: `@using Nop.Web.Framework` is all that is needed for `@Html.Action(...)`.** Deferral 7.3-1 is resolved — the bridge moved to `Nop.Web.Framework/ChildActionExtensions.cs` in namespace `Nop.Web.Framework` precisely so no `_ViewImports.cshtml` needs a new import for it. Do not duplicate it and do not add `Nop.Web.Extensions`. Note two behaviours the bridge does NOT reproduce and that are already accepted: view components and this bridge do **not** run the action-filter pipeline (deferral 7.3-6), and the `area` argument to `@Html.Widget(...)` is inert (§14.32).
    - **ALSO FROM 8.3: `MvcHtmlString` will bite in the views even though it does not appear in `Models/`.** 8.3's residual-`System.Web` scan covered the 276 `.cs` files only. Expect `MvcHtmlString.IsNullOrEmpty(x)` → `x.IsNullOrEmpty()`, `new ViewDataDictionary()` → `Html.NewViewData()`, `HttpUtility.JavaScriptStringEncode` → `JavaScriptHelper.Encode`, `Html.Partial` → `await Html.PartialAsync` (`MVC1000`), and the `TagBuilder.ToString()` trap — a `TagBuilder` concatenated into a `StringBuilder` emits the literal string `"Microsoft.AspNetCore.Mvc.Rendering.TagBuilder"`, which compiles, passes the gate, and is only visible in the rendered HTML (§15b). 7.3's §30 records each substitution with its reasoning; reuse them.
    - Port all ~325 `.cshtml` files: `WebViewPage`/`@model` base → `RazorPage<TModel>`; `@Html` `HtmlHelper` → `IHtmlHelper`; `UrlHelper` → `IUrlHelper`; `@Html.Action` → View Components
    - **Delete `Areas/Admin/Views/web.config`** and any nested `Views/web.config`; add `_ViewImports.cshtml` carrying `@using`, `@inject`, and `@addTagHelper` directives
    - **CORRECTION FROM 8.1, THEN SUPERSEDED BY 8.2: the file is now `Areas/Admin/Views/Web.config`, and the file to create is `Areas/Admin/Views/_ViewImports.cshtml`.** 8.1 correctly reported that this project had no `Areas/` directory and that the view `Web.config` was at `Views/Web.config`; **task 8.2 moved the whole tree to `Administration/Areas/Admin/Views/`** to resolve deferral 8.1-4 (the views compiled as `/Views/…` and the expander searched `/Administration/Views/…`, so nothing resolved — runtime-deferrals.md §50.1). It is still the ONLY view `Web.config` and `Areas/Admin/Views/` is still the ONLY view tree (`Shared/`, `Shared/EditorTemplates/`, `Shared/DisplayTemplates/` sit beneath it), so per deferral 7.5-1's per-tree rule **one** `Areas/Admin/Views/_ViewImports.cshtml` suffices — Razor's upward walk reaches it from every admin view, and there is no `Themes/` tree here. **CREATE IT FIRST:** on `Nop.Web` the equivalent single file took the error count from **1974 to 24** (§27), and 8.1 measured the same dependency here — the `ProductModel`(96)/`OrderModel`(52)/`DiscountModel`(24)/… `CS0246` cluster is not hundreds of separate defects, those types compile fine in `Models/` and are unresolvable *only inside views* because `Views/Web.config`'s `pageBaseType` + `<namespaces>` no longer apply. Two constraints on its content: it must **not** carry `System.Web.Optimization` (bundling is dropped — design §8), and the `System.Web.*` namespace list maps to ASP.NET Core equivalents rather than transferring verbatim. Also remove the two now-inert bundling checkboxes from `Areas/Admin/Views/Setting/GeneralCommon.cshtml` (design §8). See runtime-deferrals.md §46.3.
    - **FROM 8.1 (deferral 8.1-2) — TWO CONFIRMED CASE-SENSITIVITY DEFECTS TO FIX, both in `Areas/Admin/Views/Shared/_AdminLayout.cshtml`** (moved by 8.2; the line numbers are unchanged)**:** line 43 `Html.AppendScriptParts("~/Administration/scripts/admin.navigation.js")` → `Scripts` (the admin navigation script; it also means `IFileVersionProvider` silently emits no `?v=`), and line 99 `@Url.Content("~/administration/content/images/throbber-synchronizing.gif")` → `~/Administration/Content/images/…`. 8.1 already ran deferral 7.7-4's audits over `Administration/`: 5 `MapPath` literals clean, 116 filesystem-rooted `~/…` references with exactly these 2 mismatches, and 582 relative CSS `url(...)` refs clean. What 8.4/8.5 should still extend the scan to: `{0}`-substituted paths (the `~/Administration/Content/kendo/{0}/…` family) and paths composed by concatenation. See runtime-deferrals.md §47.
    - Re-base `_ViewStart.cshtml` and `_Layout.cshtml` on the Core view engine; port admin display/editor templates
    - **FROM TASK 7.7 (new deferral 7.7-4) — RUN THE CASE-SENSITIVITY AUDIT OVER `Administration/`.** 7.7 found **8** asset/path references in Nop.Web and Nop.Services spelled differently from the directories on disk; they resolved on Windows and 404'd/threw on Linux, and one of them killed the entire filesystem picture store. `Administration/` was excluded by 7.7's scope boundary and has ~325 views plus its own asset trees, so on this evidence it contains instances. Audit **every** `~/Content|Scripts|Themes/…` reference in admin views and stylesheets, and every `MapPath("~/…")` literal in admin controllers (`RoxyFilemanController` does its own path arithmetic), checked **case-exactly** against the filesystem — not by eye, with a script. See runtime-deferrals.md §42.4 and §44/7.7-4.
    - _Requirements: 4.9, 4.4, 4.7_
  - [x] 8.5 Move static admin assets to wwwroot / static-file middleware
    - **RESULT: the admin assets serve. `Nop.Admin` 0 errors / 16 warnings and all five upstream baselines exact (0/3 · 0/3 · 0/10 · 0/10 · 0/15) with `--no-incremental` after `rm -rf obj bin`; no warning added; `"converted to a warning"`/`ContinueOnError`/`NU1901`–`NU1904`/Six Labors lines all 0. `Nop.Tests` 4 passed. `Nop.Web.SmokeTests` **73 passed / 0 failed / 18 skipped** (was 54/0/18) — +19, all in the ALWAYS-RUN set, no new skip; `HarnessCanaryTests` now **6 failed / 0 passed**.** See runtime-deferrals.md §63–§69.
    - **DECISION: the allow-list was widened, the trees were NOT relocated under `wwwroot`** — `NopStaticFileProvider` gains `Administration/Content/**` + `Administration/Scripts/**` as a nested first-two-segment rule (NOT `"Administration"` in `AllowedRoots`, which would have exposed `db_backups`, `Areas/Admin/Views`, `sitemap.config`, `Web.config` and `bin`). Relocation was rejected on **admin-specific measured grounds**, not by inheriting 7.4's: `RoxyFilemanController`'s `confFile` reads `~/Administration/Content/Roxy_Fileman/conf.json` as a **physical path**, and `TinyMceHelper.GetTinyMceLanguage()` probes `~/Administration/Content/tinymce/langs/{culture}.js` and **falls back to English silently** on a miss. 7.4's three storefront blockers do not apply here; these two do, and relocation would also have required editing 8.6's file. See §63.
    - **THE BIG FINDING (§64) — `dotnet publish` put the admin assets in the WRONG PLACE, on top of the storefront's.** Measured: `Nop.Admin` published `Content/`, `Scripts/`, `db_backups/` and `sitemap.config` to the **publish ROOT**, not under `Administration/`. Two independent failures, both invisible from a development build and both missed by 8.1 (which checked *presence*, not *path*): (1) a published admin **404s on every asset even with the allow-list widened**, because the provider resolves against the content root, which is the publish directory in a deployment; (2) it **silently corrupts the storefront** — deferral 8.2-2's own remedy is "publish both into one directory", and the overlap is real: `Content/ionicons` is *structurally different* between the two trees, plus **12 colliding files** in `Scripts/`, twelve of which are byte-identical today, which is exactly what lets this survive review. Fixed with `Link` metadata on every publish item.
    - **§64.1 — the obvious `Link` was wrong and the audit caught it.** `Link="Administration\Content\%(RecursiveDir)…"` publishes to `Administration/Content/Content/…`, because these are `Update` rules against project-rooted default-glob items so `%(RecursiveDir)` **already carries the prefix** (measured: Identity `Content/styles.css` → RecursiveDir `Content/`). The publish audit went 16 failures → 1 → PASS, so it was **proven able to fail organically** before its PASS was believed.
    - **§64.2 — the `None` rules were not sufficient.** The Web SDK classifies **`.json` as `Content`, not `None`**, so 12 Roxy Fileman files (`conf.json` + 11 client-fetched `lang/*.json`) were split off and still published at the root path. Mirrored `Content Update=` rules added over the trees (not the 12 files) so a future `.json` cannot silently reintroduce the split. `sitemap.config` needed the same Link or **the admin menu does not render in a deployment**.
    - **DEFERRAL 8.1-1 RESOLVED (§65):** `Content/Roxy_Fileman/tmp/placeholder.txt` committed + publish include (a `.txt` is a `None` item → `Never` by default, the trap 7.4 and 8.1 both hit). Verified discriminatingly with a planted `.zip`: placeholder **present**, `.zip` **absent**. **Necessary but not sufficient** — DOWNLOADDIR stays inoperative until 8.6 fixes deferral 8.1-3, and the placeholder says so.
    - **NEW SECURITY HARDENING (§63.2): `DeniedSubpaths` refuses `Administration/Content/Roxy_Fileman/tmp`**, checked *before* any allow rule. Those temp archives hold arbitrary uploaded content and can survive an interrupted request, so serving them would bypass `[AdminAuthorize]` — the same shape as the `.bak` defect 8.3 fixed. A path exclusion rather than a `.zip` deny, because `~/Content/samples/product_*.zip` are the sample downloadable products and **must** keep serving.
    - **DEFERRAL 7.7-4, static-asset half, CLOSED — one real defect (§66).** `JbimagesController` asked for `~/content/images/uploaded/` where the directory is `Content/Images/uploaded`, so admin rich-text image upload threw `DirectoryNotFoundException` on Linux and worked on Windows. **8.1's audit missed it because that audit matched only literals written inline in a `MapPath(...)` call** — both `JbimagesController` and `RoxyFilemanController` assign to a variable first; 8.5 audits *every* `"~/…"` literal, hence 6 checked where 8.1 found 5. Final: 6 literals · 0 rooted refs in admin `.css`/`.js` · 582 relative CSS `url(...)` · 4 concatenated fragments → **0 mismatches**. Auditor proven able to fail on two of its four passes with planted canaries, both reverted.
    - **DEFERRAL 8.2-2 NARROWED, not closed (§64.3).** Verified end to end that publishing `Nop.Web` then `Nop.Admin` into one directory now yields a correct deployment — storefront `jquery.validate.min.js` checksum unchanged, `Content/ionicons/` intact, admin assets under `Administration/`, `Nop.Admin.dll` at the root where `WebAppTypeFinder` scans, and 0 `.bak`/`.cs`/`.cshtml`/`tmp *.zip`/`Settings.txt`. What remains is that **nothing enforces the two steps** — re-recorded as **deferral 8.5-1** for 18.x.
    - **TESTS (§67): new always-run fixture `AdminStaticAssetTests` (19 assertions).** Deliberately independent of `Nop.Admin.dll` — the provider resolves against the filesystem, so admin asset serving is observable without the area being routable, which sidesteps deferral **8.4-1** and makes the assertions hold in **both** store states. Covers 14 asset families (incl. the two files deferral 8.1-2 was about), the shared `max-age=604800` `<clientCache>` header, `IFileVersionProvider` cache busting *plus* a denied path coming back unversioned, 14 must-not-serve paths each **asserted to exist on disk first** (with a `.bak` and a `tmp/*.zip` planted in setup), 7 non-enumerable directories *plus* 2 that must be enumerable, and 4 mis-cased URLs. **Two known-gap tests inverted** in `InstallModeTests`. Proven able to fail four ways: a sixth permanent canary, and two temporary regressions giving **18 failed** (serving rule removed — the SECURITY tests correctly stayed green) and **2 failed** (`DeniedSubpaths` removed — exactly the two SECURITY tests).
    - **NEW DEFERRAL 8.5-1 (Medium), for 8.8 / 18.x:** nothing enforces the two-step publish; and `_AdminLayout.cshtml`'s 81 asset references are still verified only *statically* (8.4) plus 14 representatively (8.5) — nothing renders an admin page and requires every emitted `href`/`src` to return 200, the way `Deferral_40_the_install_page_only_links_assets_that_actually_serve` does for the storefront. That needs deferral **8.4-1** fixed first; **recommended for 8.8**, ~10 lines given the existing helper.
    - **OUT OF SCOPE, untouched as instructed:** `RoxyFilemanController.cs`, `ProductController.cs`, `CheckoutAttributeController.cs` (8.6, concurrent — verification therefore ran in an isolated `git worktree` at HEAD carrying only 8.5's files, because `Nop.Admin` was transiently at `CS0246: ImageFormat` in the shared tree), `Web.config` (8.7), `Validators/` (0 files changed).
    - **FROM TASK 8.4: THE BLOCKER THIS TASK WAS WARNED ABOUT IS GONE.** Deferral 8.1-2's two casing defects in `Areas/Admin/Views/Shared/_AdminLayout.cshtml` are fixed, so widening `NopStaticFileProvider`'s allow-list will now actually serve the admin navigation script and the throbber image instead of still 404ing. 8.4 also closed the admin-**view** half of deferral 7.7-4 (81 filesystem-rooted `~/…` refs → 0 mismatches, the `kendo/{0}/…` family resolved concretely, auditor proven able to fail with a planted canary). **What is still 8.5's:** the 87 admin stylesheets under `Administration/Content/` (8.1 already audited their 582 relative `url(...)` refs clean) and any asset path composed by string concatenation outside the view tree. Note also that `Areas/Admin/Views/**/*.cshtml` must stay OUT of the allow-list — 8.2's relocation did not widen that surface and neither should this task. See runtime-deferrals.md §59.5.
    - Relocate the admin `Content/` and `Scripts/` trees under `wwwroot` (or expose them through the host's static-file middleware) instead of relying on the `System.Web` handler pipeline; update view asset references accordingly
    - **FROM TASK 7.7:** the admin 404 is now confirmed by execution, not inference — the installation page links `/Administration/Content/bootstrap/css/bootstrap.min.css` and `/Administration/Content/adminLTE/AdminLTE-2.3.0.min.css`, neither of which serves, so **the installer renders unstyled today**. `Nop.Web.SmokeTests` asserts that as a known gap and will fail when this task fixes it, which is the signal to update the test. Also note deferral 7.7-4: audit the admin asset references for case-sensitivity at the same time, or a widened allow-list will still 404.
    - **FROM TASK 7.4 (new deferral 7.4-2):** the storefront half of this problem is solved and the mechanism is reusable. `Nop.Web/Infrastructure/NopStaticFileProvider.cs` is an **allow-listed** `IFileProvider` over the content root, and it deliberately excludes `Administration/`, so the admin assets currently **do not serve** — the same symptom deferral 40 described for the storefront. The cheapest correct fix is to widen that allow-list with `Administration/Content/**` and `Administration/Scripts/**` rather than relocating the trees; that also gives admin assets `IFileVersionProvider` cache busting for free, because the deferral-33 property holds for anything the provider serves. It must **not** be done by widening the root, or `Administration/db_backups` and the admin `.cshtml` tree come with it. See runtime-deferrals.md §34.
    - **FROM TASK 8.2 (new deferral 8.2-2):** `Nop.Admin.csproj`'s `CopyNopAdminToHostOutput` target drops `Nop.Admin.dll` into `Nop.Web`'s **build** output, which is what makes the admin area work for `dotnet run` and for the 8.8 gate. It does **not** reach `dotnet publish`, because publish is computed from `Nop.Web`'s own item graph — so a *published* storefront has no `Nop.Admin.dll` and therefore **no admin area at all**, silently. This task already has to decide how the admin `Content/`/`Scripts/` trees reach a deployment, so decide the assembly at the same time (publish both projects into one directory, or add a publish-time equivalent of the copy). See runtime-deferrals.md §51/8.2-2.
    - **FROM TASK 8.1:** the PUBLISH half is already done in `Nop.Admin.csproj` — `Content\**` and `Scripts\**` carry `CopyToPublishDirectory="PreserveNewest"`, because the SDK classifies `.js`/`.css`/`.png`/… as `None` (default `Never`) and without those rules a published admin has no assets at all. That is orthogonal to WHERE they are served from, which is this task's decision; if the trees are relocated under `wwwroot` those two entries become harmless no-ops. Also: widening the allow-list **without** fixing deferral 8.1-2's two casing defects in `_AdminLayout.cshtml` will still 404 — that is exactly the interaction warned about above, now with a concrete file and line numbers. And **deferral 8.1-1**: `Content/Roxy_Fileman/tmp/` does not exist (the legacy `<Folder Include>` placeholder is gone and git does not track empty directories), so `RoxyFilemanController`'s zip download throws; commit a placeholder **plus a publish include for it** (a `.txt` is a `None` item), or `Directory.CreateDirectory` before writing. See runtime-deferrals.md §46.2 and §48.
    - _Requirements: 4.7, 4.6_
  - [x] 8.6 Replace System.Drawing image processing in Nop.Admin with SixLabors.ImageSharp
    - **✅ DONE — and it was never a compile fix: `Nop.Admin` was 0 errors / 16 warnings before and is 0 errors / 16 warnings after, the same six own warnings (all 8.7's). What changed is that the imaging WORKS off Windows, where it previously could not run at all.** Measured on Linux with this solution's `System.Drawing.Common` 4.7.2 pin: `new Bitmap(4,4)` and `Graphics.FromImage(...)` throw `TypeInitializationException` → `DllNotFoundException: 'libgdiplus'` (not `PlatformNotSupportedException` — that is the 6.0+ line), so `ShowThumbnail`, `ImageResize` and `ListFiles`' dimension read were all dead. `Nop.Admin.dll`'s AssemblyRef table went from **45 refs including `System.Drawing.Common` to 44 with none**, measured on both sides by reading the metadata table after reverting to HEAD and rebuilding — and `SixLabors.ImageSharp` is now referenced. Source scan: **604 files, 0 real hits**, comment-blanking, proven with a planted canary. All five upstream baselines exact (0/3 · 0/3 · 0/10 · 0/10 · 0/15); `Nop.Tests` 4 passed. See runtime-deferrals.md §70–§74.
    - **TWO RECORDED PREDICTIONS CORRECTED.** (1) **There are NO `CA1416` warnings, before or after** — this task's own text and 8.3's handover both said to expect them. The 4.7.2 reference assembly carries **no `[SupportedOSPlatform]` annotations** (only 6.0+ does), so the analyser had nothing to flag and the defect had **no compile-time signal of any kind**; the warning count does not move. (2) **§7.18's claim that the ImageSharp licence diagnostic affects `Nop.Admin` is obsolete** — re-confirmed with ImageSharp genuinely compiled against: **0** licence lines, **0** `"converted to a warning"`, **0** `ContinueOnError`, because the 2.1.13 pin ships no `build/` targets.
    - **DEFERRALS 8.1-3 AND 8.3-3 RESOLVED — the intended target was established from evidence, not translated from the `..` (§71.1).** It is the Roxy Fileman installation directory `~/Administration/Content/Roxy_Fileman/`, on four independent grounds: the upstream handler sat one level below it (`fileman/asp_net/main.ashx`, which is what the leading `../` climbs out of); `lang/` exists there and **nowhere else in the repository**; deferral 8.1-1 independently names `Content/Roxy_Fileman/tmp/` and task 8.5 concurrently created exactly that directory; and `conf.json`'s own action URLs are written relative to it. The rule: consume the leading run of `./`/`../`, resolve the remainder against that root, normalise, and **reject anything escaping it**. **NOT what 3.90 resolved to, deliberately:** under System.Web the base was the request's directory, so `../lang/en.json` became `<root>/Admin/lang/en.json` — nonexistent — meaning 3.90's Roxy Fileman error messages were resource **keys**, not sentences. All three relative paths were broken by the .ashx→MVC port and never fixed; reproducing that would preserve a defect. **8.3's "all three callers are already inoperative" was one caller short** — `LangRes` → `ParseJSON(GetLangFile())` is live on every error path, degraded only because `ParseJSON`'s empty `catch` swallowed the throw too.
    - **FOUR colour sites, not three** (`ProductController` ×2, `CheckoutAttributeController` ×2) → `Nop.Admin/Helpers/HtmlColorHelper.cs`. Parity **measured over a 33-input matrix**, not reasoned about: identical for everything the farbtastic picker can emit (`#rrggbb`, `#rgb`, every CSS named colour, any casing). **Eleven differ — 8 tightenings, 1 loosening.** Tightened (all improvements, each previously stored verbatim into markup as invalid CSS): Windows **system** colour names `ButtonFace`/`ActiveBorder`/`Menu`, `255,0,0`, `0xFF0000`, malformed hex `#12345` (which ColorTranslator silently **mangled** to `01234500`) and `#1234567`, and `" "` whitespace (which 3.90 saved as a valid colour). **Loosened, recorded not defended:** bare hex without `#` (`FF0000`) is now accepted, so a hand-typed value can be saved that renders colourless instead of erroring — pinned by a test named `..._KNOWN_LOOSENING_...`. `ColorTranslator` was **not** throwing on Linux (it is managed for these inputs), so unlike the imaging these four were functional; they moved because they were the last thing binding a Windows-only package for a job a cross-platform library does natively. Note `SpecificationAttributeController` also owns a `ColorSquaresRgb` and has **never** validated it — pre-existing asymmetry, left alone.
    - **VERIFIED BY EXECUTION, not by compiling — new project `src/Tests/Nop.Admin.Tests`, 53 passed / 0 failed / 0 skipped (§70.5).** Drives the **real** `RoxyFilemanController` through a test subclass: real files, a real `DefaultHttpContext`, real encoded bytes read back. Asserts the dimensions 3.90 produced (200x100 asked 140x120 → **140x100**, height clamped; 140x0 → **140x70**; 50x30 asked 500x400 → **50x30**), PNG output regardless of source format, `Content-Type: image/png`, `ImageResize` 1200x600→**1000x500**, encoder chosen from the destination extension, an in-range image left **byte-identical**, and — the content assertion, not just a dimension one — that a 20px blue stripe at x=0 does **not** survive the centred 140px crop. 4 canaries, **all Failed as required**. The tests also proved they can fail on real work: the first run reported **3 failures** because the relative-path resolution climbed one level too high; that bug was caught before it shipped.
    - **NOT in `Nop.Web.SmokeTests`, deliberately (new deferral 8.6-1).** A `ProjectReference` there to `Nop.Admin` would put `Nop.Admin.dll` into the smoke-test output directory — what `WebAppTypeFinder` actually scans — so the Admin area would register and `Task_8_2_…_KNOWN_GAP` would fail. That is **deferral 8.4-1, 8.8's**, in a pass that also owns four runtime checks. The new project never boots the host and is not in `NopCommerce.sln` (18.1's, same status as `Nop.Web.SmokeTests` per deferral 7.7-2). `Nop.Web.SmokeTests` measured **73 passed / 0 failed / 18 skipped** with **6 canaries all Failed** — the +19 tests and the 6th canary are **task 8.5's** concurrent work, not this task's.
    - **BEHAVIOURAL DIFFERENCES RECORDED rather than silently accepted (§71.2):** `Convert.ToInt16` **kept** in `ImageResize` (widening it would change which inputs throw); the no-op early return **kept**, so an in-range upload is never recompressed; `GetThumbnailImage` could return a JPEG's **embedded EXIF thumbnail** (~160x120) where ImageSharp always resamples — thumbnails are now *better*, dimensions unchanged; **GIF palettes differ** (ImageSharp's encoder quantizes by default, design §7, but not byte-identically to GDI+); `ResizeMode.Stretch` **not** `Max`, deliberately unlike `PictureService`, because the explicit `Crop` already matched the aspect ratio and GDI+ produced exactly that box; `ListFiles` no longer aborts a whole directory listing on one corrupt file (`Image.Identify` returns null where `Image.FromStream` threw); the thumbnail response write is now **buffered** through `WriteAsync`, removing a latent Kestrel `AllowSynchronousIO` failure. `Image.GetThumbnailImageAbort` and `public virtual bool ThumbnailCallback()` **both dropped** per design §7. A pre-existing 3.90 defect deliberately **not** fixed: `GENERATETHUMB`'s `int.TryParse` always assigns, so a missing `width` discards the 140 default and yields `NaN`.
    - **NEW DEFERRAL 8.6-2 (Low, security observation):** `FixPath` still does not normalise `..`, so a `~/`-rooted path whose lowercase form merely *contains* `FILES_ROOT` can escape it and `CheckPath`'s prefix test still passes. Pre-existing in 3.90, unreachable by anything in tree, and the whole controller is gated on `HtmlEditorManagePictures` + `[AdminAuthorize]`. Deliberately not fixed here because normalising `FixPath` changes what a **security check** accepts, without being able to exercise the file manager end to end. The new relative branch of `MapPath` **does** normalise and enforce a ceiling.
    - **COORDINATION WITH 8.5 (concurrent):** `DownloadDir`'s missing `tmp/` directory is deferral **8.1-1** and was left untouched even though a one-line `Directory.CreateDirectory` would fix it — 8.5's task text owns the choice. 8.5 created `Content/Roxy_Fileman/tmp/placeholder.txt`, which is **exactly** where this task's `../tmp/` now resolves, so 8.1-1 is now sufficient rather than merely necessary. **Untouched as instructed:** `NopStaticFileProvider.cs`, `NopStaticFilesExtensions.cs`, the admin asset trees and their publish entries (8.5), `Web.config`/configuration (8.7), `Validators/` (8.7), `Nop.Admin.csproj` (8.5 was editing it).
    - **FOR 8.8 (§74):** the imaging is verified at the unit level but never through a real HTTP request, because Roxy Fileman needs an authenticated admin with `HtmlEditorManagePictures` and a database. Once the admin area is loadable: `a=GENERATETHUMB` should return `image/png` at the predicted dimensions; an oversize upload should land at 1000px and an in-range one byte-identical; `a=FILESLIST` over a directory containing a corrupt `.png` should list every entry with that one reporting `0x0`; and an error path (e.g. `a=UPLOAD` of a `FORBIDDEN_UPLOADS` extension) should return a **localized sentence, not the bare key `E_UploadNotAll`** — that is the observable proof deferral 8.1-3's fix reaches `LangRes`.
    - **FROM TASK 8.3 — the five `System.Drawing` sites are UNTOUCHED and DO NOT ERROR TODAY, which is worth knowing before you start.** `System.Drawing.Common` reaches the compile graph transitively (task 4.2 §10 pinned it to 4.7.2 to clear `NU1904`/CVE-2021-24112 via EPPlus), so `Bitmap`/`Graphics`/`ImageFormat`/`PixelFormat`/`InterpolationMode`/`Image.GetThumbnailImageAbort` in `RoxyFilemanController.cs` and the four `System.Drawing.ColorTranslator.FromHtml` sites in `ProductController.cs`/`CheckoutAttributeController.cs` all bind. They will throw `PlatformNotSupportedException` at runtime on Linux. Expect `CA1416` warnings once the project compiles.
    - **FROM TASK 8.3 (new deferral 8.3-3): `RoxyFilemanController.MapPath` now THROWS `NopException` for a relative path** rather than guessing a target — deferral **8.1-3**, which this task owns. Not a regression: all three callers are already inoperative (the `"../Uploads"` fallback is unreached because the shipped `conf.json` sets `FILES_ROOT` to `~/Content/Images/uploaded`; `"../tmp/"` used to fail with `DirectoryNotFoundException` because the directory did not exist — **task 8.5 committed `Content/Roxy_Fileman/tmp/placeholder.txt`, so deferral **8.1-1** is RESOLVED and the directory is now present in both build and publish output. That is necessary but NOT sufficient: the relative-path throw below is what still blocks DOWNLOADDIR, and it is yours; `VerifyAction`'s call site is commented out in 3.90). **Establish the INTENDED target — do not mechanically translate the `..`**: `~/Administration/Content/Roxy_Fileman/../Uploads` is `~/Administration/Content/Uploads`, which does not exist on disk either. 8.3 ported the rest of that file's HTTP surface (`_context`/`_r` are now the controller's own `HttpContext`/`Response`; `Request["x"]`, `Response.Write`, `TransmitFile`, `Response.End()`, `OutputStream`, the session indexer and `SaveAs` are all done), so what remains there is imaging plus this path question.
    - Rewrite `Controllers/RoxyFilemanController.cs`: `System.Drawing`/`System.Drawing.Imaging` (`Bitmap`, `Graphics`, `PixelFormat`, `InterpolationMode`) → `Image<TPixel>` + `image.Mutate(x => x.Resize(new ResizeOptions { … }))` with an ImageSharp `IResampler`; `Image.GetThumbnailImageAbort` has no counterpart and is dropped
    - Replace `System.Drawing.ColorTranslator.FromHtml(...)` colour-string validation in `Controllers/ProductController.cs` and `Controllers/CheckoutAttributeController.cs` (and equivalents) with `SixLabors.ImageSharp.Color.TryParse(...)` — no imaging pipeline required
    - _Requirements: 5.8, 5.9, 5.11, 5.3, 5.10_
  - [x] 8.7 Migrate Nop.Admin configuration and remove obsolete files
    - **✅ DONE — the CONFIGURATION half was empty as predicted, and the OBSOLETE-FILE half was not: deleting `Web.config` was entangled with a deployment-breaking defect nobody had looked for.** `Nop.Admin` **16 → 15** warnings, **0 errors**, nothing suppressed (no new `NoWarn`, no new `#pragma`). All five upstream baselines exact after `rm -rf obj bin --no-incremental`: `Nop.Core` 0/3 · `Nop.Data` 0/3 · `Nop.Services` 0/10 · `Nop.Web.Framework` 0/10 · `Nop.Web` 0/15. `Nop.Tests` 4/0; `Nop.Admin.Tests` 53/0/0 with its 4 canaries still 4 failed; `Nop.Web.SmokeTests` **74 passed / 0 failed / 18 skipped** (+1 test) with `HarnessCanaryTests` still 6 failed / 0 passed. Swallowed-diagnostics scan clean across all six `-v:n` logs. See runtime-deferrals.md §75–§77.
    - **THE DEFECT (runtime-deferrals.md §75.2): `Microsoft.NET.Sdk.Web` was GENERATING an ANCM `web.config` for this LIBRARY, naming `Nop.Admin.dll` as the application entry point — and it collides with `Nop.Web`'s.** Measured: a publish of `Nop.Admin` emitted a 485-byte shim with `<aspNetCore processPath="dotnet" arguments=".\Nop.Admin.dll" hostingModel="inprocess" />`. There is no `Main`. 8.1/8.5's `<Content Update="Web.config" CopyToPublishDirectory="Never" />` never prevented this and was never about it — it stopped the *legacy* file being COPIED; the shim is GENERATED, which is why three tasks' publish audits missed it (they checked that the legacy *content* was absent, and it was). In deferral 8.5-1's single-directory deployment this **overwrites the storefront's own 14154-byte `web.config`** (`arguments=".\Nop.Web.dll"` plus 7.4's `urlCompression` and `X-Powered-By` elements) with one that boots a library with no entry point. **AND THIS TASK'S OWN DELETION ESCALATES IT:** measured A/B, `TransformWebConfig` names its output from the project's `web.config` item when one exists and falls back to the literal lowercase name when none does — so with the legacy file present the generated name was `Web.config` (Windows-only collision) and with it deleted it is `web.config` (**collision on every filesystem, Linux included**). Fixed with `<IsTransformWebConfigDisabled>true</IsTransformWebConfigDisabled>`, the SDK's documented switch. **Verified and proven able to fail:** an 18-assertion publish audit with a planted `.bak` and a planted `Roxy_Fileman/tmp/*.zip` on disk passed 18/18, and reverting the property makes the first two assertions FAIL with the shim reappearing. The two-step publish now yields exactly ONE `web.config`, **md5-identical** to the `Nop.Web`-only publish, with `Nop.Admin.dll` at the root and admin assets under `Administration/`.
    - **`Administration/Web.config` DELETED, and its dead publish-exclusion entry REMOVED** rather than left as a no-op (as 7.5 did for `Nop.Web`, and as 8.4 already did for the view one). Element-by-element disposition in runtime-deferrals.md §75.1 — the 7 `<pages><namespaces>` entries are superseded by `Areas/Admin/Views/_ViewImports.cshtml` (task 8.4) and everything else is dropped with nothing to replace it. Worth noting one item: the `appSettings` key is literally `ClientValidationEnabled4.5.1`, a VS upgrade-wizard artifact **no code has ever read**. **NO replacement `web.config` and none needed — CONFIRMED, not assumed:** `OutputType` is `Library`, there is no `Program.cs`/`Main` anywhere under `Administration/`, and it never had a `Global.asax`; the hosted application is `Nop.Web`, whose own `web.config` carries the only two live IIS settings.
    - **NO CONFIGURATION TO MIGRATE, and no `appsettings.json` contribution needed — enumerated, not assumed (§75.3).** `ConfigurationManager`/`System.Configuration`/`System.Web.Configuration`/`MachineKey`: **zero** non-comment occurrences, confirming 8.1's count of one and 8.3's removal of it. `IConfiguration`/`NopConfigurationManager`/`GetAppSetting`/`IOptions<`: **zero**. `NopConfig` appears 3 times, all read-only or contractual (`SettingController` reads `RedisCachingEnabled`; `DependencyRegistrar.Register`'s signature). Nothing writes configuration. `appsettings.json` was **not modified** and did **not** gain a `machineKey` key.
    - **DEFERRAL 8.3-1 CLOSED BY DECISION (§75.5): no replacement `<machineKey>` warning.** Reasoning recorded at the call site with a reversal recipe: the multi-instance key-ring risk is already documented in `appsettings.json`'s `MultipleInstancesEnabled` block and under deferral 7.13, so what is missing is a UI affordance not the information; interrogating `KeyManagementOptions.XmlRepository` answers a **different** question from 3.90's auto-generated-key check and would report a security-relevant verdict from an inference about a lazily-resolved framework internal; and deferral **8.4-1** means nothing can render this page until 8.8, so it would ship unexercised. Warning unconditionally on `MultipleInstancesEnabled` was considered and rejected. Also verified §2's other item on this page: 8.3 **did** correctly make trust level report `"Full"` unconditionally, with the partial-trust guard collapsed to `!assembly.IsDynamic` — no change needed.
    - **WARNINGS — one cleared by FIXING, three decisions recorded (§75.4).** CLEARED: `CS0618` `TimeZone.CurrentTimeZone.StandardName` → `TimeZoneInfo.Local.StandardName`, **behaviour-preserving and MEASURED** by probe in two zones (`TZ` unset → both `Coordinated Universal Time`; `TZ=America/New_York` → both `Eastern Standard Time`; `DaylightName` matched in both, so the DST-aware zone really exercised it). It was the only `System.TimeZone` use left in the solution and the same method already used `TimeZoneInfo.Local` for `GetBuildDate`. KEPT `SYSLIB0014` `WebRequest.Create` — `Nop.Core/Plugins/OfficialFeedManager.cs:23` is the **identical** pattern in Nop.Core's accepted 3-warning baseline and `Nop.Services/Common/KeepAliveTask.cs:25` in Nop.Services' accepted 10, so converting only this one breaks consistency; and the conversion is **not** behaviour-preserving (per-operation vs whole-operation timeout; `GetResponse()` throws on 4xx/5xx where `HttpClient.Send` does not; a static `HttpClient` brings its own proxy/DNS). A `#pragma` was written and then **removed** — Nop.Core carries the identical warning visibly, so suppressing it here would make the counts stop describing the code. KEPT the four `CS0618` FluentValidation `Custom(...)` — **consistent with `Nop.Web`, re-measured for this decision: it carries 5 identical ones** deliberately left by 7.3. The in-version replacement determines `PropertyName` differently, so a rewrite can silently move which admin form field an error attaches to, on validators that cannot be exercised without a database, in the last task before the gate.
    - **`Properties/AssemblyInfo.cs` KEPT** (§75.6), verified independently: `GenerateAssemblyInfo=false` solution-wide means the SDK emits no replacement attributes, so deleting it drops `AssemblyVersion` `3.9.0.0` → `0.0.0.0` and loses `AssemblyTitle`/`AssemblyFileVersion`/`ComVisible`/`Guid`. **ENUMERATED RATHER THAN ASSUMED (§75.7):** `Administration/` contains exactly TWO `*.config`/`*.asax*`/`*.ashx`/`*.axd` files — `Web.config` (deleted) and `sitemap.config`. No `app.config`, no `packages.config`, no `App_Start/`, no `*Config.cs` — the same "nothing to act on" 7.5 found for `Nop.Web`. `sitemap.config` is KEPT and load-bearing (`Menu.cshtml` line 7 reads it through `CommonHelper.MapPath`; not HTTP-reachable).
    - **TEST CHANGE (§75.8): deleting `Web.config` correctly BROKE task 8.5's SECURITY test, which is that test working as designed** — it asserts each path **exists on disk first** so a 404 is a decision, and it failed with `PREMISE BROKEN: /Administration/Web.config is not on disk, so refusing it proves nothing`. Fixed by **strengthening, not by deleting coverage**: the path was removed from `mustNotServe` (its ".config must not serve" case is still carried by `sitemap.config`) and replaced with a new `Task_8_7_the_admin_project_has_no_web_config_at_any_casing`, turning "must not be served" into "must not exist". **Non-vacuous by construction** — the same search must also find `sitemap.config`, so a broken glob fails the control clause instead of satisfying the main one for free. **Both clauses proven able to fail independently:** planting `Areas/Admin/Views/web.config` → Failed; moving `sitemap.config` aside → Failed with `found 0 .config files`. Both reverted, suite re-run green.
    - **THREE NEW DEFERRALS, all Low/Medium and none blocking the gate. 8.7-3 is the one 8.8/18.x should note:** nothing automated asserts that a *publish* emits no ANCM `web.config` — the new test guards the source tree, and `IsTransformWebConfigDisabled` guards generation, but the defect was invisible for three tasks precisely because no check looked at publish output for a file nobody authored; removing the property would reintroduce it silently while the source-tree test still passed. **8.7-1**: three `WebRequest`/`WebClient` `SYSLIB0014` sites remain across `Nop.Core`, `Nop.Services` and `Nop.Admin` — convert all three together or none, and decide the timeout semantics explicitly. **8.7-2**: nine FluentValidation `Custom(...)` `CS0618` sites remain (4 admin + 5 storefront) and will grow through groups 10–17; retire the family in one pass alongside any revisit of design §9's 7.6.105 pin.
    - **OUT OF SCOPE, untouched as instructed:** `NopStaticFileProvider.cs` and the admin asset trees / publish `Link` metadata (8.5), `RoxyFilemanController.cs` / `ProductController.cs` / `CheckoutAttributeController.cs` / `Helpers/HtmlColorHelper.cs` (8.6), and deferrals **8.3-2** / **8.4-1** (8.8). In particular 8.7 did **not** add a `ProjectReference` from `Nop.Web.SmokeTests` to `Nop.Admin`: that would start registering the Admin area and make `Task_8_2_…_KNOWN_GAP` fail, which is 8.8's inversion to make in one pass with its own assertions.
    - _Requirements: 1.4, 1.5, 5.3_
    - **FROM TASK 8.3: THERE IS NO CONFIGURATION LEFT TO MIGRATE HERE — the one `ConfigurationManager` call site was a REMOVAL and 8.3 already did it.** `CommonController.cs`'s `ConfigurationManager.GetSection("system.web/machineKey") as MachineKeySection` is gone, along with `using System.Configuration;` and `using System.Web.Configuration;`. `MachineKeySection` does not exist on .NET in any form and `<machineKey>` has **no successor setting** — Data Protection's key ring is a file/registry/blob store configured in code, not a config section with a literal key — so **`appsettings.json` must NOT grow a `machineKey` key**. The two `Admin.System.Warnings.MachineKey.*` localization resources are now orphaned, which is harmless. **What 8.7 must DECIDE (new deferral 8.3-1):** whether to reinstate an equivalent warning by interrogating Data Protection's `IXmlRepository` for a machine-local key ring. The underlying multi-instance risk is real and already recorded under deferral 7.13 (auth cookies stop validating across instances without a shared key ring, and 7.4 §32.1 notes it beside `MultipleInstancesEnabled`), but surfacing it is a new diagnostic rather than a port.
    - **ALSO FROM 8.3: `Properties/AssemblyInfo.cs` MUST BE KEPT** — task 8.1 §46.4 already corrected this task's text. `Directory.Build.props` sets `GenerateAssemblyInfo=false` solution-wide (task 2.3), so the SDK emits **no** replacement attributes and deleting the file drops `AssemblyVersion` from `3.9.0.0` to `0.0.0.0`. Task 7.5 §37.1 measured exactly this for `Nop.Web`. Moving to SDK-generated assembly info is a solution-wide change (flip the property, add `Version`/`FileVersion`/`Title`, delete all six files together), not a per-project one.
    - **ALSO FROM 8.3: two pre-existing warnings in `CommonController.cs`/`HomeController.cs` are newly unmasked and deliberately left** — `TimeZone.CurrentTimeZone` (`CS0618`) and `WebRequest.Create` (`SYSLIB0014`). Both are 3.90 code that works, neither is a `System.Web` dependency, and both are non-blocking (Req 3.3). This task owns whether to modernise them.
    - Move any admin-owned settings to `IConfiguration`/typed options; rewrite `ConfigurationManager.*` call-sites
    - **CORRECTION FROM 8.1: there is exactly ONE `ConfigurationManager` call site and it is a REMOVAL, not a migration.** `Controllers/CommonController.cs` line 458 does `ConfigurationManager.GetSection("system.web/machineKey") as MachineKeySection` for the admin System Info page. `MachineKeySection` does not exist on .NET in any form — ASP.NET Core replaced `<machineKey>` with Data Protection — so there is no setting to carry across and `appsettings.json` must **not** grow a `machineKey` key. Either delete the block or report the Data Protection key-ring state instead, which is the genuinely useful equivalent and ties to deferral 7.13's multi-instance key-ring caveat. Same file, second item already recorded in runtime-deferrals.md §2: lines 205 and 217 must report trust level `"Full"` unconditionally.
    - Remove obsolete files: `Properties/AssemblyInfo.cs`, `app.config`/`web.config` fragments owned by the admin project, and any App_Start-style registration files superseded by the ASP.NET Core model
    - **CORRECTION FROM 8.1: DO NOT remove `Properties/AssemblyInfo.cs`.** Same measured reason task 7.5 recorded for `Nop.Web` (runtime-deferrals.md §37.1): `Directory.Build.props` sets `GenerateAssemblyInfo=false` solution-wide (task 2.3), so the SDK emits **no** replacement attributes — deleting it drops `AssemblyVersion` from `3.9.0.0` to `0.0.0.0` and loses `AssemblyTitle`/`AssemblyFileVersion`/`ComVisible`/`Guid`. All five migrated projects keep theirs; moving to SDK-generated assembly info is a solution-wide change, not a per-project one. There is **no `app.config`** and **no App_Start-style file** in this project (checked, not assumed). What 8.7 does own is deleting `Web.config` — MVC5 `<namespaces>`, a `<compilation targetFramework="4.5.1"/>`, five System.Web/OWIN `appSettings` keys and a `<runtime><assemblyBinding>` block, none readable on .NET 10; its own comment says it exists only "to make razor intellisense work". 8.1 already excluded it and the view `Web.config` (now at `Areas/Admin/Views/Web.config` after 8.2's move) from **publish** as the interim measure 7.4 used, so once both files are gone those two `Content Update` entries in `Nop.Admin.csproj` become no-ops and can be removed, exactly as task 7.5 removed 7.4's equivalents. See runtime-deferrals.md §46.4 and §46.5.
    - _Requirements: 1.4, 1.5, 5.3_
  - [x] 8.8 Clean-compile gate — Nop.Admin
    - **✅ DONE — GATE PASSED. `Nop.Admin` 0 errors / 15 warnings, and all six projects re-gated exact with `--no-incremental` after `rm -rf obj bin`:** `Nop.Core` 0/3 · `Nop.Data` 0/3 · `Nop.Services` 0/10 · `Nop.Web.Framework` 0/10 · `Nop.Web` 0/15 · `Nop.Admin` 0/15 — **no warning added**, even though this task edited `Nop.Core` and `Nop.Web.Framework`. Swallowed-diagnostics check on all six `-v:normal` logs: `"converted to a warning"` **0** · `ContinueOnError` **0** · `NU1901`–`NU1904` **0** · `error MSB*` **0** · `error NETSDK*` **0** · Six Labors licence lines **0**. Source scan: **604 files (278 `.cs` + 326 `.cshtml`), 0 real hits** over 55 legacy tokens, comment-blanking and **proven able to fail** with planted canaries. `Nop.Admin.dll` AssemblyRef **metadata table** (not a text search): `Nop.Admin v3.9.0.0`, 44 refs, **0 banned**, likewise for the other five assemblies, **proven able to fail** via an `--also-ban` switch. Tests: `Nop.Tests` **4/0**; `Nop.Admin.Tests` **53/0/0** with 4 canaries all Failed; `Nop.Web.SmokeTests` **148 passed / 0 failed / 13 skipped** (was 74/0/18, **+74 passing**) with 8 canaries all Failed. **Honest exception, reported not hidden:** `System.Drawing.Common` 4.7.2 remains in `deps.json` through the single edge `EPPlus/4.5.3.3 → System.Drawing.Common`, pinned by task 4.2 §10 to clear CVE-2021-24112; it is **absent from every assembly's reference table** and from `Nop.Admin`'s output directory. See runtime-deferrals.md §76–82.
    - **THREE REAL DEFECTS FOUND, TWO OF THEM BLOCKERS.** (1) **The admin UI could not render a single page** — two area bugs in `Nop.Web.Framework/ChildActionExtensions.cs`: the child `RouteData` dropped `area`, so child views were resolved through the NON-AREA location formats (`Html.Action('NopCommerceNews','Home')` searched `/Views/Home/…` and died); and `FindAction` matched on controller+action NAME only, so `_AdminLayout.cshtml` invoked **`Nop.Web`'s** `CommonController.LanguageSelector` and died on the model-type mismatch. Both fixed; failability measured — both reverted fails **23 of 29** `AdminUiRenderTests`, the route-data half alone fails 13, and **the `FindAction` half alone fails NOTHING** (recorded as deferral 8.8-1: descriptor enumeration currently favours the admin candidate by accident, which MVC does not guarantee). (2) **Deferral 8.2-1** — fixed and verified, see below. (3) An assertion that had **never executed** (`Deferral_7_3_4_a_former_child_action_is_no_longer_reachable_by_URL`, installed-store only) was **wrong**: the `PageNotFound` page renders the storefront layout, so `class="footer"` is present in a *correct* 404 — corrected to test for a full document instead. See runtime-deferrals.md §77.
    - **DEFERRAL 8.4-1 RESOLVED FIRST, as required — and deliberately not the convenient way.** `Nop.Web.SmokeTests.csproj` gains a **build-order-only** `ProjectReference` to `Nop.Admin` (`ReferenceOutputAssembly="false"`) plus an explicit copy target, so `Nop.Admin.dll` is in the test host's `BaseDirectory` and **absent from `deps.json`** — reproducing production exactly (`Nop.Web.deps.json` also has 0 occurrences). A plain `ProjectReference` was rejected because it would resolve the assembly from the trusted-platform-assemblies list, i.e. verify the admin area through a route production never takes, in the exact respect that produced the defect. This also **closes an honest limit 8.2 recorded about itself**: `Task_8_2_WebAppTypeFinder_loads_base_directory_assemblies_without_throwing` is now a real assertion. Proven able to fail: removing the DLL fails **65 tests / 79 pass**, every admin assertion red and every storefront one green. See runtime-deferrals.md §76.
    - **DEFERRAL 8.2-1 FIXED HERE rather than left for group 10** — same one-line change as §50.3, and group 10 would have hit it on its first plugin. `PluginManager.PerformFileDeploy` now uses `AssemblyLoadContext.Default.LoadFromAssemblyPath`. **Verified with a REAL plugin**, not a probe: a new permanent minimal `src/Tests/Nop.Plugin.SmokeProbe/` is planted under the content root's `Plugins/` before the host starts and discovered by the real `PluginManager.Initialize()`; `PluginDiscoveryTests` asserts it loads, loads **from `~/Plugins/bin`**, is absent from both `deps.json` and the test output directory, is in the **default** load context with its type still assignable to `IPlugin`, and becomes an MVC `ApplicationPart`. Proven able to fail: reverting throws `FileNotFoundException` **out of host startup**. `Nop.Core` re-gated 0/3. See runtime-deferrals.md §77.3.
    - **DEFERRAL 8.7-3 RESOLVED — and 8.7's suggested technique would have produced a VACUOUS test.** Measured: `ComputeFilesToPublish -getItem:ResolvedFileToPublish` reports **0** `web.config` mentions in *both* states, because `TransformWebConfig` writes the file directly rather than contributing an item. Reported instead of written. The guard is `VerifyNopAdminPublishesNoWebConfig` in `Nop.Admin.csproj`, hooked `AfterTargets="_TransformWebConfig"` after three measured attempts (a `Publish` hook runs too early; `Publish;_TransformWebConfig` never fires the second hook because MSBuild runs a target at most once per build; and a literal `<Include>` creates the item whether the file exists or not, which made the first version fail in both states). Verified: publish passes with the property `true`, **fails** with `false` (485 bytes, `arguments=".\Nop.Admin.dll"`). See runtime-deferrals.md §77.2.
    - **DEFERRAL 8.3-2 RESOLVED (§78).** `Task_8_2_…_KNOWN_GAP` **inverted**, not deleted. All 16 admin cases added to the three `Deferral_7_3_4_*` invariants, and **every case is now area-qualified** — the `/__smoke/action` probe gained an `area` filter because `Common.LanguageSelector` and `Widget.WidgetsByZone` exist in BOTH areas, so an unqualified admin assertion could be satisfied by the storefront's endpoint. **The "marked name shared with an unmarked one" case 8.3-2 asked for DOES exist and was found by measurement after a wrong test failed:** `Nop.Admin.Controllers.CommonController.PopularSearchTermsReport` is marked in its parameterless form and **unmarked** in its `[HttpPost](DataSourceRequest)` form — 3.90 had the identical shape (verified against git `9cb503f`), so the Kendo grid action must stay reachable while its sibling must not. It is the only such case in either project. `BackupFileDownload` authorization asserted with a **separate unauthenticated client**, plus the 3.90 static `.bak` path still 404.
    - **EVERY REMAINING "STILL INFERENCE" ITEM FROM 8.2 IS NOW MEASURED (§78):** `Nop.Admin.dll` loads from the base directory into the **default** context; contributes **both** `AssemblyPart` **and** `CompiledRazorAssemblyPart`; **326** compiled identifiers under `/Areas/Admin/Views/` (8.2 predicted 325 — task 8.4 *added* `_ViewImports.cshtml`; the test asserts the compiled count **equals the on-disk count**, which is self-maintaining); **0** views under the pre-8.2 `/Administration/` path; **54/54** controllers inherit `[Area("Admin")]` across 1070 actions; exactly one `Admin/` route pattern with 3.90's name/prefix/defaults; `GET /Admin/` reaches `Nop.Admin.Controllers.HomeController.Index`; and `CopyNopAdminToHostOutput` **does** fire in a real `Build`.
    - **TASK 8.4 §62's FOUR CHECKS, DEFERRAL 8.5-1's SECOND HALF AND TASK 8.6 §74 — all in the new `AdminUiRenderTests` (29 tests, against a real installed store with an authenticated admin).** Tab bodies render **inside** their `tab-pane` wrappers on 4 pages (covering all 78 `@helper` → `Capture` conversions at once); `Menu.cshtml` renders from the physical `sitemap.config` with its **nested** level, i.e. the recursive `Capture`; three datepicker `id`s match their `kendoDatePicker()` selectors; **every** emitted asset `href`/`src` on 3 admin pages returns 200; Roxy Fileman `DIRLIST` reads `conf.json`, `GENERATETHUMB` returns a real **PNG** over HTTP and `FILESLIST` reports non-zero dimensions — on Linux, where the `System.Drawing` pipeline throws `DllNotFoundException: libgdiplus`, so a green run *is* the cross-platform assertion. **HONEST RESULT on §62 check (4):** the Shared-before-controller shadowing quirk **cannot** be exercised against the real view set — no file name appears both in `Areas/Admin/Views/Shared/` and a controller folder — so the test pins the **absence** instead, and 8.2's probe plus the location-format assertion remain the evidence. See runtime-deferrals.md §79.
    - **FOUR NEW DEFERRALS, all Low.** **8.8-1**: the `FindAction` area preference is not independently pinned (see above) — do not delete it because nothing goes red. **8.8-2**: `Nop.Plugin.SmokeProbe` joins `Nop.Web.SmokeTests` (7.7-2) and `Nop.Admin.Tests` (8.6-1) as projects `NopCommerce.sln` does not reference — **18.1 should add all three**, and none must become part of a gate. **8.8-3**: the admin UI has only been rendered as a single-language, sample-data, single-administrator store, so multi-store/vendor/non-English rendering and `TinyMceHelper`'s silently-failing culture probe are unexercised. **8.8-4**: `BaseController`'s public methods appear as ~270 endpoints (pre-existing, MVC 5 did the same, permission-gated; a one-attribute `[NonAction]` fix if anyone cares).
    - Build `src/Presentation/Nop.Web/Administration/Nop.Admin.csproj -c Debug` using the **containerized .NET 10 SDK command in `build-environment.md`** (the host runs Amazon Linux 2 / glibc 2.26 and cannot execute .NET 10 directly); substitute the `/workspace`-relative project path
    - Gate: **zero compiler errors** (warnings non-blocking); verify no `System.Web*`, `ImageResizer`, or `System.Drawing` references remain
    - **✅ PLUGINS ARE NOW UNBLOCKED — both the 7.6 and 8.8 gates report zero errors.** Before starting group 10, read runtime-deferrals.md **§82** ("what groups 10–15 should know before the first plugin"): 8.2-1 is fixed and continuously exercised, deferral 1.2's `AssemblyPart` half is exercised but the **compiled-Razor-views** half is not (the probe plugin has no views), `IRouteProvider` is `void RegisterRoutes(IEndpointRouteBuilder)` with §17.4a's four gotchas, and **`Html.Action` is now area-aware** so a plugin action whose controller+action name collides across areas will get its own area's action.
    - _Requirements: 3.1, 3.2, 3.3, 4.2, 4.7_

- [ ] 9. Checkpoint — presentation ported
  - Ensure **all three** presentation projects — Nop.Web.Framework, Nop.Web, **and Nop.Admin** — clean-compile on ASP.NET Core with all System_Web_Dependencies replaced, the `Admin` area route preserved via `MapAreaControllerRoute`, and no `ImageResizer`/`System.Drawing` usage remaining. Ensure all tests pass, ask the user if questions arise.
  - _Requirements: 3.1, 3.3, 4.1, 4.2, 4.7, 4.10, 5.8_

- [ ] 10. Migrate DiscountRules and ExchangeRate plugins
  - **READ runtime-deferrals.md §82 FIRST — "what groups 10–15 should know before the first plugin".** Both gates (7.6 and 8.8) now pass, so plugins are unblocked.
  - **✅ DEFERRAL 8.2-1 IS FIXED AND VERIFIED — DO NOT RE-LITIGATE IT, AND DO NOT REVERT IT.** It was HIGH and would have killed the host at startup on this very task's first plugin: `PluginManager.PerformFileDeploy` loaded each shadow-copied assembly with `Assembly.Load(AssemblyName.GetAssemblyName(path))`, and on .NET the default `AssemblyLoadContext` binds from the `deps.json`-derived trusted-platform-assemblies list and does **not** probe directories — `~/Plugins/bin` is in no `deps.json`, so it threw `FileNotFoundException`, which `PluginManager.Initialize` rethrows out of startup. **Task 8.8 changed it to `AssemblyLoadContext.Default.LoadFromAssemblyPath(...)` and verified it with a real planted plugin** (`src/Tests/Nop.Plugin.SmokeProbe/`, exercised continuously by `Nop.Web.SmokeTests.PluginDiscoveryTests`), proving it able to fail by reverting the line. The **default** context is mandatory: a separate context gives the plugin its own copy of every `Nop.Core` type, so `typeof(IPlugin).IsAssignableFrom(t)` is false and the plugin is discovered, loaded and then **silently ignored**. See runtime-deferrals.md §77.3.
  - **DEFERRAL 1.2 IS ONLY HALF EXERCISED.** A loaded plugin *does* become an MVC `ApplicationPart` — asserted with the real probe plugin. But that plugin has **no Razor views**, so only the `AssemblyPart` half is proven; the compiled-Razor-views half (`ConsolidatedAssemblyApplicationPartFactory`, task 8.2's reason for using `ApplicationPartFactory` rather than `new AssemblyPart(...)`) is **first exercised by the first plugin in this group that ships a `.cshtml`**. If a plugin's views fail to resolve, that is where to look — not at the route provider.
  - **`Html.Action` IS NOW AREA-AWARE (task 8.8).** `ChildActionExtensions` prefers candidates in the caller's area, falling back to all areas. A plugin admin view invoking a storefront action still works, but a plugin that declares an action whose controller+action name already exists in the other area will now get **its own area's** action — correct MVC 5 behaviour, and possibly different from before. It lives in `Nop.Web.Framework`, namespace `Nop.Web.Framework`, which every `_ViewImports.cshtml` already imports, so no new `@using` is needed. See runtime-deferrals.md §77.1.
  - **ALSO FROM 8.2 (deferral 8.2-3):** the admin view tree moved to `Administration/Areas/Admin/Views/`, so every plugin view that references `~/Administration/Views/Shared/_AdminPopupLayout.cshtml` or `~/Administration/Views/Shared/_GridPagerMessages.cshtml` must become `~/Areas/Admin/Views/Shared/…`. **12 sites across 11 files**, listed with line numbers in runtime-deferrals.md §51/8.2-3 — they affect tasks 10.2, 11.2, 13.1, 14.4 and 15.1. It must stay an explicit path: these are cross-assembly references to views compiled into `Nop.Admin.dll`.
  - **`IRouteProvider` is `void RegisterRoutes(IEndpointRouteBuilder)`** — §17.4a has the mechanical recipe and its four gotchas, of which two bite hardest: `string[] namespaces` has no counterpart and is simply dropped, and equal-precedence patterns now raise `AmbiguousMatchException` where MVC 5 resolved by registration order. **`.WithOrder(1000)` is NOT the fix** — task 7.3 measured that it *creates* the ambiguity; use `SuppressMatchingMetadata` for a route that exists only for URL generation (§28.1).
  - [x] 10.1 Migrate Nop.Plugin.DiscountRules.CustomerRoles
    - SDK-style net10.0; packages.config → PackageReference; config migration; remove obsolete files; rewrite unavailable APIs (controllers/views/DI as applicable); fix `ProjectReference`s to migrated projects; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 5.1, 5.3, 3.1_
  - [x] 10.2 Migrate Nop.Plugin.DiscountRules.HasOneProduct
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 5.1, 5.3, 3.1_
  - [x] 10.3 Migrate Nop.Plugin.ExchangeRate.EcbExchange
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 5.1, 5.3, 3.1_
  - **✅ GROUP 10 COMPLETE — 0 errors each; the pattern below is what groups 11–15 apply. Full reasoning in runtime-deferrals.md §83–§86.** Verified: `Nop.Web.SmokeTests` **126 passed / 0 failed / 52 skipped** without a database and **164 / 0 / 14** against an installed store with all three plugins installed, where all three plugin views were **rendered over HTTP**; `Nop.Tests` 4, `Nop.Admin.Tests` 53, `HarnessCanaryTests` **10 failed / 0 passed**; six upstream projects re-gated at their exact baselines with no warning added.
  - **⚠️ ONE REGRESSION WAS FOUND AND FIXED IN THE GATED `Nop.Web.Framework` (§83.4). Do not revert it.** Task 8.2 removed the two non-area `~/Administration/Views/…` view-location formats as unmatchable. The *paths* were dead; the *role* was live — 3.90 had them so a controller **outside** the Admin area could resolve an admin view by bare name, and plugin admin controllers are not in the Admin area. `Areas/Admin/Views/Shared/_AdminPopupLayout.cshtml` renders `@await Html.PartialAsync("Notifications")` by bare name, so **every plugin admin popup answered HTTP 500** until the pair was restored (repointed at `/Areas/Admin/Views/…`, 3.90's order — controller-specific first — and 3.90's position, last). Six of the 20 plugins use that layout; groups 11–15 inherit the fix and need do nothing.

- **THE PLUGIN RECIPE (established by 10.1–10.3; applies to every sub-task in groups 11–15).**
  - **Read `src/Plugins/Nop.Plugin.DiscountRules.CustomerRoles/Nop.Plugin.DiscountRules.CustomerRoles.csproj` first** — it is the annotated template, and `Nop.Plugin.ExchangeRate.EcbExchange.csproj` is the viewless variant. Four of its decisions fail **silently** if copied wrongly: no compiler error, no startup error, just a view that does not render or a plugin that is discovered and then ignored.
  - **(1) SDK.** Views → `Sdk="Microsoft.NET.Sdk.Razor"` **plus `<AddRazorSupportForMvc>true</AddRazorSupportForMvc>`** plus `<StaticWebAssetsEnabled>false</StaticWebAssetsEnabled>`. **Without `AddRazorSupportForMvc` the views still compile but the assembly carries no `[ProvideApplicationPartFactory]`**, so MVC gets an `AssemblyPart` only and every view is unresolvable (deferral 1.2's failure mode, measured). No views → plain `Microsoft.NET.Sdk`; `ExchangeRate.EcbExchange` is the **only** such plugin (38 `.cshtml` across the other 19). Do **not** use `Microsoft.NET.Sdk.Web` — it defaults `OutputType` to `Exe`.
  - **(2) VIEW RESOLUTION — one `ItemGroup`, and 3.90's `View("~/Plugins/…")` call sites stay UNEDITED.** `<Content Update="Views\**\*.cshtml" Link="Plugins\<ShortName>\%(RecursiveDir)%(Filename)%(Extension)" CopyToOutputDirectory="Never" />`. `Link` is what the Razor SDK's `AssignTargetPath` uses as the compiled identifier, so the views compile as `/Plugins/<ShortName>/Views/…` — byte-identical to 3.90. **`%(RecursiveDir)` already contains `Views/`; do not repeat it.** Also add `Views/_ViewImports.cshtml` (`@inherits Nop.Web.Framework.ViewEngines.Razor.WebViewPage<TModel>` + the namespaces from the deleted `web.config`) — it must be inside the same glob, or Razor resolves imports from a different directory and they silently do not apply.
  - **(3) `Private="false"` ON EVERY `ProjectReference` **AND** the `NopPluginDoNotDeployHostAssemblies` target.** The metadata alone is **not** sufficient: a transitive `Nop.Data.dll` leaks into the plugin folder (measured), and `PluginManager` shadow-copies and loads it as the process's `Nop.Data`. Copy the target verbatim; it filters on the `Nop.` prefix so `Payments.PayPalDirect`'s private PayPal SDK (the only `<Private>True</Private>` in the 20 legacy projects, task 12.3) still deploys.
  - **(4) `OutputPath` = 3.90's `..\..\Presentation\Nop.Web\Plugins\<ShortName>\` **AND** `<AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>`.** Without the second the plugin deploys to `Plugins\<ShortName>\net10.0\`, whose parent is not `Plugins`, so `PluginManager.IsPackagePluginFolder` rejects it and the plugin is invisible.
  - **`Description.txt`/`logo.jpg`/`Notes.txt` need `<None Remove>` + `<Content Include>`, not `<Content Update>`.** Under the Razor SDK the default `Content` glob covers only `.cshtml`/`.razor`; `.txt`/`.jpg` arrive as `<None>`, so an `Update` silently matches nothing and the plugin ships without its `Description.txt`. Assert `SupportedVersions` contains `NopVersion.CurrentVersion` (`3.90`) rather than assuming it.
  - **DELETE** `web.config`, `app.config`, `packages.config` and the `Microsoft.Bcl.Build` import + its `EnsureNuGetPackageBuildImports` guard. **KEEP `Properties/AssemblyInfo.cs`** — `GenerateAssemblyInfo=false` is solution-wide, so deleting it drops `AssemblyVersion` to `0.0.0.0`.
  - **`IRouteProvider`** is `void RegisterRoutes(IEndpointRouteBuilder)`; drop the `string[] namespaces` argument, keep 3.90's route **names** (several plugin views resolve URLs by name), keep `Priority` at 0. Worked examples: the two `RouteProvider.cs` files in group 10.
  - **A plugin's routes only exist when the plugin is INSTALLED** — `RoutePublisher` skips an uninstalled plugin's provider (3.90's filter). Do not read a missing endpoint as a porting defect.
  - **Substitutions seen in group 10, all reusing §30/§55:** `System.Web.Mvc` → `Microsoft.AspNetCore.Mvc` (+ `Microsoft.AspNetCore.Mvc.Rendering` for `SelectListItem`); `Json(x, JsonRequestBehavior.AllowGet)` → `Json(x)`; `[AllowHtml]`/`[ValidateInput(false)]` **deleted** (no ASP.NET Core counterpart — a recorded security-relevant relaxation, deferral 7.3-3); `HttpUtility.JavaScriptStringEncode` → `JavaScriptHelper.Encode`; `Html.Partial` → `await Html.PartialAsync`.
  - **Verification:** add each plugin as a build-order `ProjectReference` (`ReferenceOutputAssembly="false" ExcludeAssets="all" PrivateAssets="all"`) in `src/Tests/Nop.Web.SmokeTests`, and extend `PluginViewRenderTests` — its `/__smoke/plugins` probe already reports discovery, both application-part types, compiled identifiers, view-engine lookups, `_ViewStart` isolation, deployment shape and endpoints for any plugin added to its three name constants.
  - **✅ DEFERRAL 10.x-1 IS RESOLVED BY TASK 11.1 — DO NOT RE-DO IT.** `NopStaticFileProvider`'s allow-list now has a **third**-level `Plugins/*/{Content,Scripts}/**` rule plus `Plugins/bin` in `DeniedSubpaths`, so `~/Plugins/<ShortName>/Content/*.css` and `Scripts/*.js` serve while the plugin's assembly, `.pdb`, `.deps.json` and `Description.txt` stay refused. Covers `ExternalAuth.Facebook`, `Feed.GoogleShopping` **and `Widgets.NivoSlider`** (15.3). A plugin shipping a static tree still has to **deploy** it: `<None Remove="Content\**" />` + `<Content Include="Content\**" CopyToOutputDirectory="Always" />`, because under the Razor SDK `.css`/`.js`/`.jpg` arrive as `<None>` and a bare `Update` matches nothing. Serving is asserted over real HTTP by `Nop.Web.SmokeTests.PluginStaticAssetTests`; add your paths to its `MustServe`. See runtime-deferrals.md §92.2.
  - **✅ DEFERRAL 11.x-1 (found and fixed at 11.1) — THE `Html.Action` BRIDGE IS NOW CONSTRAINT-AWARE AND BINDS COMPLEX PARAMETERS. DO NOT REVERT IT.** It was **Medium-High and silent**: a plugin's admin `Configure` form posts back to the *admin* URL, so the POST arrives as another child-action render, and the bridge selected the parameterless GET overload and passed `null` for the settings model — **every plugin settings form discarded the administrator's input with no error**. `ChildActionExtensions` now reproduces MVC 5's `ActionMethodSelector` (`[HttpPost]`, `[FormValueRequired]`, …, as a preference with a fallback) and binds complex parameters through the controller's own `TryUpdateModelAsync` over the application's real value provider factories. Affects every plugin with a settings form. See runtime-deferrals.md §92.1.
  - **✅ DEFERRAL 4.10 — the shared `GO`-batch helper is `Nop.Data.DbContextExtensions.SplitSqlIntoBatches` / `ExecuteSqlScript(this DbContext, string)`.** A plugin context's `Install()` calls `this.ExecuteSqlScript(CreateDatabaseScript())`; do not re-derive a splitter. Worked EF6→EF Core plugin-context example: `Nop.Plugin.Feed.GoogleShopping/Data/GoogleProductObjectContext.cs` (11.2). Still owed by 13.1, 14.4, 15.1. See runtime-deferrals.md §92.3.

- [ ] 11. Migrate ExternalAuth and Feed plugins
  - **APPLY THE PLUGIN RECIPE established by group 10** — the block immediately after task 10.3, and the annotated `Nop.Plugin.DiscountRules.CustomerRoles.csproj` it points at. Four decisions there fail silently if copied wrongly: `AddRazorSupportForMvc=true`, the `Content`/`Link` block, `Private="false"` **plus** the `NopPluginDoNotDeployHostAssemblies` target, and `AppendTargetFrameworkToOutputPath=false`. Full reasoning: runtime-deferrals.md §83.
  - **DEFERRAL 8.2-3 — 1 site here:** `Feed.GoogleShopping/Views/Configure.cshtml:279` names `~/Administration/Views/Shared/_GridPagerMessages.cshtml`; it becomes `~/Areas/Admin/Views/Shared/…` and `Html.Partial` becomes `await Html.PartialAsync`. Keep it an explicit `~/`-rooted path — it is a cross-assembly reference into `Nop.Admin.dll` (verified by execution at 10.2).
  - **DEFERRAL 10.x-1 IS YOURS TO FIX — Medium, fails silently.** Both plugins ship a `Content/` tree referenced as `~/Plugins/<ShortName>/Content/…` (`ExternalAuth.Facebook/Views/PublicInfo.cshtml:5`, `Feed.GoogleShopping/Views/Configure.cshtml:4`), and `NopStaticFileProvider`'s allow-list does not include `Plugins/`, so those URLs 404 with nothing reported. Recommended remedy (a nested second-level allow-list entry, as task 8.5 did for `Administration/`) in runtime-deferrals.md §85/10.x-1. `Widgets.NivoSlider` (15.3) depends on the same fix.
  - **`ExternalAuth.Facebook` is the one plugin with a real third-party dependency to decide about:** its legacy `<Reference>`s name `DotNetOpenAuth.AspNet`/`.Core`/`.OAuth`/`.OAuth.Consumer`/`.OpenId`/`.OpenId.RelyingParty` 4.3.0, which have no net10.0 build and no successor. Expect to port the OAuth flow to `Microsoft.AspNetCore.Authentication.Facebook` (in the shared framework) rather than to substitute a package, and record it as a behavioural change.
  - [x] 11.1 Migrate Nop.Plugin.ExternalAuth.Facebook
    - Apply per-project recipe (port OAuth/controllers/views to ASP.NET Core); fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 5.1, 5.3, 3.1_
  - [x] 11.2 Migrate Nop.Plugin.Feed.GoogleShopping
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 5.1, 5.3, 3.1_
  - **✅ GROUP 11 COMPLETE — 0 errors each, 0 warnings of their own (10 upstream each, the same set as group 10's DiscountRules pair). Full reasoning in runtime-deferrals.md §91–§94.** Verified: six upstream projects re-gated at their exact baselines (`Nop.Core` 0/3 · `Nop.Data` 0/3 · `Nop.Services` 0/10 · `Nop.Web.Framework` 0/10 · `Nop.Web` 0/15 · `Nop.Admin` 0/15) with **no warning added**; group 10's three plugins rebuilt at 10/10/11; `Nop.Tests` **4**, `Nop.Admin.Tests` **53**, `Nop.Web.SmokeTests` **164 passed / 0 failed / 58 skipped** without a database (**+26 tests from this group**: 14 in `PluginViewRenderTests`, 12 in the new `PluginStaticAssetTests`), `HarnessCanaryTests` **13 failed / 0 passed**. Residual `System.Web*`/`DotNetOpenAuth`/`HttpRuntime`/`@helper` scan across 37 files: **0 real hits**, scanner proven able to fail with canaries in both `.cs` and `.cshtml`.
  - **⚠️ A THIRD SILENT DEFECT WAS FOUND, IT IS IN THE GATED `Nop.Web.Framework`, AND IT AFFECTS EVERY REMAINING PLUGIN GROUP. DO NOT REVERT IT — NEW DEFERRAL 11.x-1 (§92.1).** The `Html.Action` bridge matched on action+controller **name** and broke ties by *fewest parameters*, ignoring `[HttpPost]` and `[FormValueRequired]`, and handed every **complex** parameter `null`. A plugin's admin `Configure` form posts back to the **admin** URL, so the POST arrives as another child-action render — which means **pressing Save on any plugin settings page re-ran the parameterless GET and discarded the administrator's input, with no error anywhere.** `Feed.GoogleShopping` is the sharpest case: its "Save" and "Generate feed" buttons are two `[HttpPost] Configure` actions with **one parameter each**, distinguished only by `[FormValueRequired]`, so the tie-break was also capable of running the wrong one. `ChildActionExtensions` now reproduces MVC 5's `ActionMethodSelector` (constraint-aware, as a preference with a fallback) and model-binds complex parameters through the controller's own `TryUpdateModelAsync`. **Affects 12.1–12.5, 13.1, 14.4, 15.1, 15.2, 15.3 — every plugin with a settings form.** Pinned by four tests in `PluginViewRenderTests`, each proven able to fail by reverting the mechanism.
  - **✅ DEFERRAL 10.x-1 RESOLVED for all three affected plugins, INCLUDING 15.3.** `NopStaticFileProvider` gained a **third**-level `Plugins/*/{Content,Scripts}/**` rule plus `Plugins/bin` in `DeniedSubpaths`. `Scripts/` is in the rule, so **task 15.3 has nothing left to do here** — the `Scripts` half is pinned with a planted file by `PluginStaticAssetTests.Task_11_1_the_Scripts_half_of_the_rule_works_for_15_3`. See §92.2.
  - **✅ DEFERRAL 4.10 RESOLVED for 11.2, and the SHARED HELPER lives in `Nop.Data`** — `DbContextExtensions.SplitSqlIntoBatches(string)` plus the `ExecuteSqlScript(this DbContext, string)` extension a plugin's `Install()` calls. `CreateTablesIfNotExist` was refactored onto the same method so there is one implementation. **Tasks 13.1, 14.4 and 15.1 must call `this.ExecuteSqlScript(CreateDatabaseScript())`** in place of 3.90's `Database.ExecuteSqlCommand(dbScript)`. See §92.3.
  - **DEFERRAL 8.2-3: 1 site fixed, 9 remain** (13.1 ×3, 14.4 ×4, 15.1 ×2). §51's register is still the authority.

- [x] 12. Migrate Payments plugins
  - **APPLY THE PLUGIN RECIPE established by group 10** — the block immediately after task 10.3, and the annotated `Nop.Plugin.DiscountRules.CustomerRoles.csproj` it points at. Four decisions there fail silently if copied wrongly: `AddRazorSupportForMvc=true`, the `Content`/`Link` block, `Private="false"` **plus** the `NopPluginDoNotDeployHostAssemblies` target, and `AppendTargetFrameworkToOutputPath=false`. Full reasoning: runtime-deferrals.md §83.
  - **`Payments.PayPalDirect` is the ONLY plugin in the solution that deliberately deploys a private third-party assembly** — the PayPal 1.8.0 SDK, the single `<Private>True</Private>` in all 20 legacy project files. The recipe's `NopPluginDoNotDeployHostAssemblies` target filters on the `Nop.` prefix precisely so that keeps working; verify the dll actually lands in `Plugins\Payments.PayPalDirect\`.
  - `Payments.Manual` and `Payments.PayPalDirect` also reference `FluentValidation` directly; declare it as a version-less `PackageReference` (central package management), not a `<Reference>`.
  - Each of these ships a `PaymentInfo.cshtml` rendered as a **child action** through `IPaymentMethod`'s action/controller/`RouteValueDictionary` triple (deferral 7.3-1), i.e. through the `Html.Action` bridge in `Nop.Web.Framework`, which is now **area-aware** (§77.1). All five set `Layout = ""`, which ASP.NET Core honours.
  - [x] 12.1 Migrate Nop.Plugin.Payments.CheckMoneyOrder
    - Apply per-project recipe (controllers/views/DI to ASP.NET Core); fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 4.4, 5.1, 5.3, 3.1_
  - [x] 12.2 Migrate Nop.Plugin.Payments.Manual
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 4.4, 5.1, 5.3, 3.1_
  - [x] 12.3 Migrate Nop.Plugin.Payments.PayPalDirect
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 4.4, 5.1, 5.3, 3.1_
  - [x] 12.4 Migrate Nop.Plugin.Payments.PayPalStandard
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 4.4, 5.1, 5.3, 3.1_
  - [x] 12.5 Migrate Nop.Plugin.Payments.PurchaseOrder
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 4.4, 5.1, 5.3, 3.1_
  - **✅ GROUP 12 COMPLETE — 0 errors each. Full reasoning in runtime-deferrals.md §91–§94.** Verified: `Nop.Web.SmokeTests` **164 passed / 0 failed / 58 skipped** (task 12 contributes **+18**: 16 pass, 2 skip needing a store); `Nop.Tests` 4; `Nop.Admin.Tests` 53; `HarnessCanaryTests` **13 failed / 0 passed** (was 10 — three added); the six upstream projects re-gated at their exact baselines (0/3 · 0/3 · 0/10 · 0/10 · 0/15 · 0/15) with **no warning added**, and group 10's three plugins rebuilt at 0 errors. Own warnings: 0 for CheckMoneyOrder/Manual/PurchaseOrder, 2 `SYSLIB0014` for PayPalStandard, 1 `NU1701` for PayPalDirect. Residual `System.Web` scan: **0 real hits across 53 files**, canary-proven in both languages and both directions.
  - **THREE DEFECTS FOUND BY EXECUTION, each invisible at compile time. Do not "simplify" any of the three fixes.**
    - **(1) `Request.Form` on a GET throws — HTTP 500 on the checkout page (§87.1).** 3.90's `PaymentInfo` child action read `this.Request.Form` unconditionally; `System.Web` returned an empty collection for a request with no form body, ASP.NET Core **throws**. Three of the five do this (`Manual`, `PayPalDirect`, `PurchaseOrder`), and `PaymentInfo` is rendered as a child action on the **GET** of `/checkout/paymentinfo`, so it is not an edge case — it is every arrival at the payment step. Fixed with the `HasFormContentType` guard, the pattern already used at eight sites in this migration. **Groups 13–15: any plugin action that reads `Request.Form` outside a `[HttpPost]` needs the same guard.**
    - **(2) THE RECIPE DOES NOT DEPLOY A PLUGIN'S PACKAGE ASSETS AT ALL (§87.2).** Task 10.1's template says the `NopPluginDoNotDeployHostAssemblies` `"Nop."` scoping is what lets `Payments.PayPalDirect`'s private PayPal SDK deploy. **That is true and NOT SUFFICIENT, and it had never been exercised** — no group-10 plugin has a package runtime asset. `$(CopyLocalLockFileAssemblies)` is **`false` for a class library**, so `PayPal.dll` was simply absent, while the plugin still built clean, was discovered, reported compatible and could be installed. A new `NopPluginDeployPayPalSdk` target copies exactly that one package's assets (selected by `NuGetPackageId`, with an `<Error>` guard). **Any later plugin that needs a third-party dll deployed inherits this problem** — 14.x's shipping plugins are the ones to check.
    - **(3) DEFERRAL 7.3-2 WAS LIVE, and would have corrupted a stored order field (§87.2a).** Exactly one of the five writes `ProcessPaymentRequest.CustomValues` (`PurchaseOrder`, one entry). On ASP.NET Core the form indexer returns `StringValues`, which boxes into the `Dictionary<string, object>` and serializes as a JSON **array**, so `Order.CustomValuesXml` would have recorded the literal text `["PO-1234"]` — displayed to the customer, in the confirmation e-mail and in the admin. Fixed with an explicit `.ToString()`; the counterfactual is measured in the same fixture so the fix cannot be removed silently. **`TypeNameHandling` was NOT used** (task 4.2 refused it as a deserialization-gadget hazard).
  - **CORRECTION to this task group's brief:** `IPaymentMethod` in 3.90 has **no** `PaymentInfoActionName`/`PaymentInfoControllerName`/`ButtonPaymentMethodActionNames` — those are 4.x names. The 3.90 contract is the out-parameter triple `GetConfigurationRoute`/`GetPaymentInfoRoute(out string, out string, out RouteValueDictionary)`, and that is what was verified (§87.3). The substance was right: it IS one of deferral 7.3-1's five dynamically-named `Html.Action` sites, and it now renders over HTTP.
  - **DECISIONS RECORDED, all consistent with prior tasks:** the PayPal package is **deprecated and has no netstandard asset in any version** — pinned at 3.90's 1.8.0 with `NU1701` left visible (§87.2); `HttpWebRequest` in `PayPalStandard` **kept**, 2 × `SYSLIB0014` left visible, the fifth consecutive time this migration has declined the `HttpClient` conversion, and the reason matters most here because `VerifyIpn` is a payment-authenticity check (§87.4); `IPNHandler` and `WebhookEventsHandler` became **`async`** because Kestrel's `AllowSynchronousIO` is `false` (§87.6); `Encoding.ASCII` and the always-200 webhook reply **preserved deliberately** (§87.6); `Request.IsLocal` reproduced **inside the plugin** rather than promoted into the concurrently-edited `Nop.Web.Framework` (§87.8).
  - **NEW DEFERRAL 12.x-2, MEDIUM, THROWS AT RUNTIME — pre-existing, found here, not fixed here.** Five synchronous `Request.Body.CopyTo` reads in `Nop.Web`/`Nop.Admin` (`ReturnRequestController`, `ShoppingCartController` ×2, `DownloadController`, `PictureController`) will throw `InvalidOperationException` under Kestrel's default `AllowSynchronousIO = false`. File-and-line register in runtime-deferrals.md §89/12.x-2.
  - **NOTE FOR 13.1–15.3 on the smoke tests:** the payment assertions live in **new** files — `src/Tests/Nop.Web.SmokeTests/PaymentPluginTests.cs` and `PaymentPluginProbe.cs` (a `/__smoke/payments` probe) — rather than in `PluginViewRenderTests`/`WritePluginsProbe`, because group 11 was editing those concurrently. The change to `SmokeProbeMiddleware` is one `case` line. Either pattern is fine to copy; the new probe additionally reports the `IPaymentMethod` triple's resolution, every non-plugin dll in a deployment directory, and whether a .NET Framework facade type is loadable in the host.

- [ ] 13. Migrate Pickup plugin
  - **APPLY THE PLUGIN RECIPE established by group 10** — the block immediately after task 10.3, and the annotated `Nop.Plugin.DiscountRules.CustomerRoles.csproj` it points at. Four decisions there fail silently if copied wrongly: `AddRazorSupportForMvc=true`, the `Content`/`Link` block, `Private="false"` **plus** the `NopPluginDoNotDeployHostAssemblies` target, and `AppendTargetFrameworkToOutputPath=false`. Full reasoning: runtime-deferrals.md §83.
  - **DEFERRAL 4.10 — YOUR CONTEXT IS `Data/StorePickupPointObjectContext.cs`, AND THE HELPER ALREADY EXISTS (task 11.2, §92.3).** Replace 3.90's `Database.ExecuteSqlCommand(CreateDatabaseScript())` in `Install()` with **`this.ExecuteSqlScript(CreateDatabaseScript())`** — `Nop.Data.DbContextExtensions.ExecuteSqlScript`, which splits on `GO` through `Nop.Data.DbContextExtensions.SplitSqlIntoBatches`. Do NOT re-derive a splitter: `CreateTablesIfNotExist` was refactored onto that same method so there is exactly one. `src/Plugins/Nop.Plugin.Feed.GoogleShopping/Data/GoogleProductObjectContext.cs` is the worked EF6→EF Core plugin-context example, including why `ApplyConfigurationsFromAssembly` must be passed **this** assembly (otherwise the model gains all ~105 `Nop.Data` entities and `Install()` scripts the whole nopCommerce schema) and which `IDbContext` members stay `NotImplementedException`.
  - **DEFERRAL 11.x-1 IS FIXED — your `Configure` POST now works, and did not before (§92.1).** The `Html.Action` bridge is constraint-aware and model-binds complex parameters. Nothing to do; do not revert it.
  - **DEFERRAL 8.2-3 — 3 sites here:** `Views/Configure.cshtml:57` (`_GridPagerMessages.cshtml`), `Views/Create.cshtml:2` and `Views/Edit.cshtml:2` (`_AdminPopupLayout.cshtml`).
  - **`Views/Configure.cshtml` SETS NO `Layout` AT ALL** — the one view in the whole solution that relies on no `_ViewStart` applying. That property is preserved by the recipe's `Content`/`Link` block (the identifier stays under `/Plugins/…`, whose ancestors have no `_ViewStart`) and it is the reason the views were NOT relocated under `/Views/<Controller>/`, where `Nop.Web/Views/_ViewStart.cshtml` would have wrapped it in the storefront's one-column layout. See runtime-deferrals.md §83.1. If you change the view layout of this plugin, re-read that section first.
  - [x] 13.1 Migrate Nop.Plugin.Pickup.PickupInStore
    - Apply per-project recipe (controllers/views/DI to ASP.NET Core); fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 4.4, 5.1, 5.3, 3.1_

- [x] 14. Migrate Shipping plugins
  - **APPLY THE PLUGIN RECIPE established by group 10** — the block immediately after task 10.3, and the annotated `Nop.Plugin.DiscountRules.CustomerRoles.csproj` it points at. Four decisions there fail silently if copied wrongly: `AddRazorSupportForMvc=true`, the `Content`/`Link` block, `Private="false"` **plus** the `NopPluginDoNotDeployHostAssemblies` target, and `AppendTargetFrameworkToOutputPath=false`. Full reasoning: runtime-deferrals.md §83.
  - **DEFERRAL 8.2-3 — 4 sites, all in 14.4 `Shipping.FixedOrByWeight`:** `Views/AddRateByWeightPopup.cshtml:2` and `Views/EditRateByWeightPopup.cshtml:2` (`_AdminPopupLayout.cshtml`), `Views/_ByWeight.cshtml:71` and `Views/_FixedRate.cshtml:61` (`_GridPagerMessages.cshtml`).
  - **DEFERRAL 4.10 — 14.4's context is `Data/ShippingByWeightObjectContext.cs`, AND THE HELPER ALREADY EXISTS (task 11.2, §92.3).** Replace `Database.ExecuteSqlCommand(CreateDatabaseScript())` in `Install()` with **`this.ExecuteSqlScript(CreateDatabaseScript())`** (`Nop.Data.DbContextExtensions`, which splits on `GO` via `SplitSqlIntoBatches`). Do not re-derive a splitter. Worked example: `Nop.Plugin.Feed.GoogleShopping/Data/GoogleProductObjectContext.cs`.
  - **DEFERRAL 11.x-1 IS FIXED (§92.1)** — 14.4's `Configure` POST reaches its `[HttpPost]` overload with a bound model, which it did not before task 11.1. Do not revert it.
  - `Shipping.AustraliaPost` references `Newtonsoft.Json` directly — version-less `PackageReference`. `Shipping.FixedOrByWeight` also carries `EntityFramework`/`EntityFramework.SqlServer` `<Reference>`s: those are EF6 and arrive as EF Core transitively through `Nop.Data`; do not restate them.
  - [x] 14.1 Migrate Nop.Plugin.Shipping.AustraliaPost
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - **DONE — 0 errors.** SDK-style `Microsoft.NET.Sdk.Razor` csproj per the 10.1 recipe (all five silently-failing decisions applied); deleted app/packages/web.config; added `Views/_ViewImports.cshtml`; controller `System.Web.Mvc`→`Microsoft.AspNetCore.Mvc` + `[ChildActionOnly]`→`[NopChildActionOnly]`; computation method `System.Web.Routing`→`Microsoft.AspNetCore.Routing`. **`Newtonsoft.Json` is a version-less `<PackageReference PrivateAssets="all">`** (central 13.0.4 pin). **`WebRequest`/`HttpWebRequest` KEPT** (the JSON GET relies on `WebException.Response` in its catch — HttpClient.Send would not throw on 4xx/5xx); `SYSLIB0014` left visible, consistent with the five prior WebRequest carriers. Deploy verified: only own dll/pdb + Description.txt/logo.jpg — no `Nop.*`, no `Newtonsoft.Json.dll`, no views. Compiled view identifier at `/Plugins/Shipping.AustraliaPost/Views/Configure.cshtml`.
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 5.1, 5.3, 3.1_
  - [x] 14.2 Migrate Nop.Plugin.Shipping.CanadaPost
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - **DONE — 0 errors.** Same recipe. `CanadaPostHelper.Request` **keeps `WebRequest`** — it reads `ex.Response` in `catch (WebException)` to deserialize the carrier's XML `<messages>` error body, which HttpClient.Send would break; `SYSLIB0014` visible. No third-party `<PackageReference>` (parses reply with `System.Xml.Serialization`). 28 pre-existing `CS8981` warnings are the lowercase XSD-generated `Domain/{discovery,messages,rating,track}.cs` types — a faithful port does not rename generated carrier DTOs. Deploy clean.
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 5.1, 5.3, 3.1_
  - [x] 14.3 Migrate Nop.Plugin.Shipping.Fedex
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - **DONE — 0 errors. SOAP: see the prominent record in runtime-deferrals.md §14.x-1.** FedEx was reached via TWO WSDL-generated `SoapHttpClientProtocol` proxies — `RateService` (`Web References/RateServiceWebReference/Reference.cs`, ~16,700 lines) and `TrackService` (`TrackService.cs`, ~4,850 lines). `SoapHttpClientProtocol` has no net10.0 counterpart. **Following the 4.2 precedent** (EU VAT ASMX proxy → hand-built SOAP over HttpClient): the DTO classes are plain `[XmlType]` serializable types that compile unchanged, so **only the two proxy classes were replaced** — by a `RateService`/`TrackService` calling `FedexSoapInvoker.cs`, which reproduces the EXACT SOAP 1.1 / document-literal-bare wire shape (action `http://fedex.com/ws/rate/v16/getRates` and `track`, request/reply element names and operation namespaces transcribed from the generated method attributes) — **the carrier request shape is preserved because it is an external contract.** Async machinery removed (unused). `System.Web.Services.Protocols.SoapException` → plugin-local `SoapException.cs` (same `XmlNode Detail` shape, read by both catch sites). Deleted `Properties/Settings.{settings,Designer.cs}` (`System.Configuration.ApplicationSettingsBase`; only the removed proxy read the default URL, always overridden by `FedexSettings.Url`). Controller/model ported. Deploy clean.
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 5.1, 5.3, 3.1_
  - [x] 14.4 Migrate Nop.Plugin.Shipping.FixedOrByWeight
    - Apply per-project recipe (controllers/views to ASP.NET Core); fix `ProjectReference`s; clean-compile
    - **DONE — 0 errors. The group-14 outlier: own DbContext + RouteProvider + 6 views.** DbContext `Data/ShippingByWeightObjectContext.cs` ported EF6→EF Core following the §92.3 GoogleShopping worked example — preserved the load-bearing `(string nameOrConnectionString)` ctor (RegisterPluginDataContext constructs it reflectively), `ApplyConfigurationsFromAssembly(GetExecutingAssembly())` confining the model to the one table, and **`Install()` now calls `this.ExecuteSqlScript(CreateDatabaseScript())`** (the SHARED `Nop.Data.DbContextExtensions` helper from task 11.2 — deferral **4.10**'s 14.4 quarter RESOLVED, splitter not re-derived). `ShippingByWeightRecordMap` → `Configure(EntityTypeBuilder<...>)` preserving `ToTable("ShippingByWeight")` and `Zip HasMaxLength(400)`. **`Data/EfStartUpTask.cs` DELETED** (EF Core has no initializer pipeline). `RouteProvider` → `RegisterRoutes(IEndpointRouteBuilder)` + `MapControllerRoute`, names/patterns 3.90's. Controller: dropped the `Initialize(RequestContext)` override (culture is now set globally by `WorkingCultureMiddleware`, matching migrated Nop.Admin), `this.Request.RawUrl`→`this.Request.GetEncodedPathAndQuery()` (×4, the §51 pattern), `[ChildActionOnly]`→`[NopChildActionOnly]`, `JsonRequestBehavior.AllowGet` dropped. **Deferral 8.2-3 — all 4 sites RESOLVED** (see below). No `Nop.Data.dll` leaked (the sharpest leakage case, since this references Nop.Data directly). Deploy clean.
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 4.4, 5.1, 5.3, 3.1_
  - [x] 14.5 Migrate Nop.Plugin.Shipping.UPS
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - **DONE — 0 errors. Hybrid: rating over `WebRequest`, tracking over SOAP.** Rating (`UPSComputationMethod.DoRequest`) **keeps `WebRequest`** (XML POST); `SYSLIB0014` visible. Tracking used the `SoapHttpClientProtocol` `TrackService` proxy (`Web References/track/Reference.cs`); **replaced only the proxy class** with a hand-built `TrackService`→`UpsSoapInvoker.cs`, following 4.3. **UNLIKE FedEx, UPS sends `UPSSecurity` as a SOAP HEADER** (`[SoapHeader("UPSSecurityValue")]`): the invoker serializes it into `<soap:Header>`, and `UPSSecurity`'s `: SoapHeader` base was dropped (plain `[XmlRoot]` type). Wire shape preserved (action `http://onlinetools.ups.com/webservices/TrackBinding/v2.0`, body `TrackRequest`, reply `TrackResponse`, header ns `.../UPSS/v1.0`). Default track URL `https://wwwcie.ups.com/webservices/Track` transcribed from the deleted `Settings.Designer.cs` into the hand-built proxy. Plugin-local `SoapException.cs`. Controller/model ported. Deploy clean.
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 5.1, 5.3, 3.1_
  - [x] 14.6 Migrate Nop.Plugin.Shipping.USPS
    - Apply per-project recipe; fix `ProjectReference`s; clean-compile
    - **DONE — 0 errors.** Same recipe. `USPSComputationMethod.DoRequest` **keeps `WebRequest`** (`WebRequestMethods.Http.Post` + `GetResponse()`); `SYSLIB0014` visible. No third-party `<PackageReference>` (parses reply with `System.Xml`). Controller `[ChildActionOnly]`→`[NopChildActionOnly]`, computation method `System.Web.Routing`→`Microsoft.AspNetCore.Routing`. Deploy clean.
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 5.1, 5.3, 3.1_

- [x] 15. Migrate Tax and Widgets plugins
  - **APPLY THE PLUGIN RECIPE established by group 10** — the block immediately after task 10.3, and the annotated `Nop.Plugin.DiscountRules.CustomerRoles.csproj` it points at. Four decisions there fail silently if copied wrongly: `AddRazorSupportForMvc=true`, the `Content`/`Link` block, `Private="false"` **plus** the `NopPluginDoNotDeployHostAssemblies` target, and `AppendTargetFrameworkToOutputPath=false`. Full reasoning: runtime-deferrals.md §83.
  - **DEFERRAL 8.2-3 — 2 sites, both in 15.1 `Tax.FixedOrByCountryStateZip`:** `Views/_CountryStateZip.cshtml:80` and `Views/_FixedRate.cshtml:60` (`_GridPagerMessages.cshtml`).
  - **DEFERRAL 4.10 — 15.1's context is `Data/CountryStateZipObjectContext.cs`, AND THE HELPER ALREADY EXISTS (task 11.2, §92.3).** Replace `Database.ExecuteSqlCommand(CreateDatabaseScript())` in `Install()` with **`this.ExecuteSqlScript(CreateDatabaseScript())`** (`Nop.Data.DbContextExtensions`, which splits on `GO` via `SplitSqlIntoBatches`). Do not re-derive a splitter. Worked example: `Nop.Plugin.Feed.GoogleShopping/Data/GoogleProductObjectContext.cs`.
  - **✅ DEFERRAL 10.x-1 IS ALREADY FIXED — 15.3 HAS NOTHING TO DO HERE (task 11.1, §92.2).** `NopStaticFileProvider` gained a third-level `Plugins/*/{Content,Scripts}/**` rule, and **`Scripts/` is in it** — the half only `Widgets.NivoSlider` needs is pinned with a planted file by `PluginStaticAssetTests.Task_11_1_the_Scripts_half_of_the_rule_works_for_15_3`, precisely so 15.3 inherits a proven rule rather than discovering it at render time. `Plugins/bin` is separately denied. **What 15.3 DOES still owe:** deploying the assets, i.e. the `<None Remove="Content\**" />` + `<Content Include="Content\**" CopyToOutputDirectory="Always" />` pair (and the same for `Scripts\**`) — under `Microsoft.NET.Sdk.Razor` the default `Content` glob covers only `.cshtml`/`.razor`, so `.css`/`.js`/`.jpg` arrive as `<None>` and a bare `Update` matches nothing. Add both paths to `PluginStaticAssetTests.MustServe`. Also note `NivoSliderPlugin.cs:77` does a `CommonHelper.MapPath` **physical read** of `~/Plugins/Widgets.NivoSlider/Content/nivoslider/sample-images/`, so the casing on disk is load-bearing twice over.
  - **DEFERRAL 11.x-1 IS FIXED (§92.1)** — the `Configure`/settings POSTs in 15.1, 15.2 and 15.3 reach their `[HttpPost]` overloads with a bound model, which they did not before task 11.1. Do not revert it.
  - **`Widgets.NivoSlider/Views/PublicInfo.cshtml` USES `@helper`, which does not exist in ASP.NET Core (RZ1002).** It is the last of the two such files in the solution; task 11.2 converted the other (`Feed.GoogleShopping/Views/Configure.cshtml`). If the helper's result is passed as an ARGUMENT, use `@functions { async Task X() { … } }` + `Capture(X)` — `WebViewPage.Capture(Func<Task>)`, task 8.4's seam — and note `async Task` rather than `void` is required, not stylistic (a `void` conversion produces `MVC1006` + `CS4033`). If it is invoked as a statement, a plain `void` method is enough (task 7.3's technique).
  - Both widget plugins render through `IWidgetPlugin`'s action/controller/`RouteValueDictionary` triple, i.e. the `Html.Action` bridge (deferral 7.3-1), which is now area-aware (§77.1) — a widget action whose controller+action name collides across areas now resolves to its own area's.
  - [x] 15.1 Migrate Nop.Plugin.Tax.FixedOrByCountryStateZip
    - Apply per-project recipe (controllers/views to ASP.NET Core); fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 4.4, 5.1, 5.3, 3.1_
  - [x] 15.2 Migrate Nop.Plugin.Widgets.GoogleAnalytics
    - Apply per-project recipe (widget view components/views to ASP.NET Core); fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 4.4, 5.1, 5.3, 3.1_
  - [x] 15.3 Migrate Nop.Plugin.Widgets.NivoSlider
    - Apply per-project recipe (widget view components/views to ASP.NET Core); fix `ProjectReference`s; clean-compile
    - _Requirements: 5.5, 1.1, 1.2, 1.3, 1.4, 1.5, 2.4, 4.2, 4.3, 4.4, 5.1, 5.3, 3.1_

- [ ] 16. Checkpoint — all plugins migrated
  - Ensure all 20 plugin projects clean-compile as SDK-style net10.0 with references to migrated projects only. Ensure all tests pass, ask the user if questions arise.
  - _Requirements: 5.2, 5.5, 3.1, 3.3, 2.4_

- [ ] 17. Migrate remaining Test projects
  - [x] 17.1 Migrate Nop.Web.MVC.Tests to SDK-style net10.0
    - Convert `src/Tests/Nop.Web.MVC.Tests`; packages.config → PackageReference (test SDK + runner); replace System.Web-based test seams with `DefaultHttpContext`/ASP.NET Core test host; fix `ProjectReference`s to migrated Nop.Web/Nop.Web.Framework and shared Nop.Tests; require clean compile
    - **RESULT: 0 errors / 5 warnings, and the suite RUNS: 143 tests, 143 passed / 0 failed / 0 skipped.** RhinoMocks 3.6.1 → **NSubstitute 6.2.0** (USER-APPROVED; BSD-3-Clause — the brief's "verify MIT" was wrong, corrected in runtime-deferrals.md §17.1) across the six files that used it. `AdminAuthorizeAttributeTests` ported to the ASP.NET Core filter shape (`AuthorizationFilterContext` / `ChallengeResult`, `Substitute.ForPartsOf` + `Configure()`), proven able to fail by flipping the expected result (3 tests go red). The three MVC-5 `RouteTable`-based routing files were **dropped** — their coverage already lives in `Nop.Web.SmokeTests` as real endpoint-routing assertions (referenced, not duplicated). Two runtime findings: a harness gap (`EngineContext` unset for validators' `[NopResourceDisplayName]` and for `EventsTests` event delivery — fixed with an assembly `[SetUpFixture]` + a self-managed `EngineContext.Replace` in `EventsTests`) and **one real defect fixed** (Nop.Admin `AdminMapperConfiguration`: `MediaSettingsModel→MediaSettings` left `AzureCacheControlHeader` unmapped, which modern AutoMapper's strict validation rejects). All six upstream projects re-gated at their recorded baselines (Core 0/3, Data 0/3, Services 0/10, Framework 0/10, Web 0/15, Admin 0/15); the other three test projects still pass (Nop.Tests 4, Nop.Admin.Tests 53, Nop.Web.SmokeTests 184/0/58); two plugins spot-checked at 0 errors. No `System.Web*`/`Rhino.Mocks` in source (comment-blanking scan, canary-proven) or in the emitted assembly's ref table. **This project is NOT added to `NopCommerce.sln` here (18.1's job); it SHOULD join as a non-gating test project — see runtime-deferrals.md §17.1 / flagged for 18.1.** See runtime-deferrals.md §17.1.
    - _Requirements: 5.6, 1.1, 1.2, 1.3, 4.2, 5.3, 3.1_

- [ ] 18. Update solution file and verify full-solution build
  - [ ] 18.1 Update NopCommerce.sln to reference migrated projects
    - Update every project entry to the migrated SDK-style `.csproj` (including `Nop.Admin`); prune the removed SQL CE data provider / any dropped project from the solution
    - **FROM TASK 7.7 (new deferral 7.7-2): add `src/Tests/Nop.Web.SmokeTests/Nop.Web.SmokeTests.csproj`.** It is a new project, so no legacy entry exists to update. It must **NOT** be added to a clean-compile gate: task 7.7 is non-gating by design and 13 of its 49 tests require a reachable SQL Server (they `Assert.Ignore` with an explicit reason when there is none, so `dotnet test` stays green without a database).
    - _Requirements: 5.4_
  - [ ] 18.2 Full-solution clean-compile verification
    - Build `src/NopCommerce.sln -c Debug` using the containerized .NET 10 SDK command in `build-environment.md`; require zero errors across all 31 in-scope projects (3 libraries, 3 presentation projects including Nop.Admin, all 20 plugins, all 5 test projects); verify no residual `System.Web*`, `ImageResizer`, or `System.Drawing` references and no remaining `packages.config`
    - _Requirements: 5.2, 5.4, 3.1, 3.3, 1.3, 5.8_

- [ ] 19. Final checkpoint — migration complete
  - Ensure the full solution clean-compiles on net10.0 in strict dependency order with complete coverage and no shortcuts. Ensure all tests pass, ask the user if questions arise.
  - _Requirements: 2.1, 2.2, 3.1, 3.3, 5.2, 5.4_

## Notes

- Tasks marked with `*` are optional test-migration/smoke sub-tasks and can be skipped for a faster path to a compiling application; core migration tasks are never optional. **Amendment (user decision):** tasks **4.5** (shared `Nop.Tests` helper project) and **7.7** (ASP.NET Core host smoke-check) were **promoted from optional to required** and no longer carry the `*` marker. Rationale: skipping every test-related task would mean no ported code is ever *executed* before the end of the migration, so the plan retained no executable validation at all. 4.5 provides a runnable test project (test SDK + runner) that later stages build on, and 7.7 provides the one end-to-end check that the ported host actually starts. Tasks **2.6**, **3.4** and **4.4** remain `*` optional and are **skipped**.
- The design defines **no Correctness Properties** (migration is a structural/declarative transformation), so no property-based test tasks are included; verification is by clean-compile gate, structural checks, and a few representative smoke checks.
- Every project group ends with an explicit **clean-compile gate**; a dependent group MUST NOT begin until its upstream gate reports zero errors (Req 2.1–2.3, 3.1–3.3).
- **All clean-compile gates run inside the `mcr.microsoft.com/dotnet/sdk:10.0` container documented in `build-environment.md`.** The host is Amazon Linux 2 (glibc 2.26) and .NET 10 requires glibc 2.27+, so the host `dotnet` CLI cannot build `net10.0`. Use `/workspace`-relative project paths in the containerized command.
- Tasks 7 (Nop.Web) and 8 (Nop.Admin) are **sibling groups** with no reference between them; both unlock after the 6.6 gate and may run in parallel, but **plugins require both the 7.6 and 8.8 gates to pass** (design §6).
- Task group 8 (Nop.Admin: 277 `.cs` files, 325 `.cshtml` views) is the **highest-iteration stage** — plan for a repeated `build → fix → build` loop, not a single pass.
- Each task references the specific requirement acceptance-criteria numbers it satisfies for traceability.
- SQL Server Compact has no EF Core provider and is intentionally removed from scope (design §8); SQL Server support is retained.
- `ImageResizer` and `System.Drawing` are replaced by `SixLabors.ImageSharp` across `Nop.Services` (4.1/4.2) and `Nop.Admin` (8.6), which keeps every project on cross-platform `net10.0` rather than `net10.0-windows` (design §7).

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 0, "tasks": ["1.1"] },
    { "id": 1, "tasks": ["2.1"] },
    { "id": 2, "tasks": ["2.2", "2.3"] },
    { "id": 3, "tasks": ["2.4"] },
    { "id": 4, "tasks": ["2.5"] },
    { "id": 5, "tasks": ["2.6", "3.1"] },
    { "id": 6, "tasks": ["3.2"] },
    { "id": 7, "tasks": ["3.3"] },
    { "id": 8, "tasks": ["3.4", "4.1"] },
    { "id": 9, "tasks": ["4.2"] },
    { "id": 10, "tasks": ["4.3"] },
    { "id": 11, "tasks": ["4.4", "4.5", "6.1"] },
    { "id": 12, "tasks": ["6.2", "6.3", "6.5"] },
    { "id": 13, "tasks": ["6.4"] },
    { "id": 14, "tasks": ["6.6"] },
    { "id": 15, "tasks": ["7.1", "8.1"] },
    { "id": 16, "tasks": ["7.2", "8.2"] },
    { "id": 17, "tasks": ["7.3", "7.4", "7.5", "8.3"] },
    { "id": 18, "tasks": ["7.6", "8.4"] },
    { "id": 19, "tasks": ["8.5", "8.6", "8.7"] },
    { "id": 20, "tasks": ["8.8"] },
    { "id": 21, "tasks": ["7.7", "10.1", "10.2", "10.3", "11.1", "11.2", "12.1", "12.2", "12.3", "12.4", "12.5", "13.1", "14.1", "14.2", "14.3", "14.4", "14.5", "14.6", "15.1", "15.2", "15.3"] },
    { "id": 22, "tasks": ["17.1"] },
    { "id": 23, "tasks": ["18.1"] },
    { "id": 24, "tasks": ["18.2"] }
  ]
}
```
